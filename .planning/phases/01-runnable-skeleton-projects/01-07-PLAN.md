---
phase: 01-runnable-skeleton-projects
plan: 07
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - frontend/src/i18n/language.ts
  - frontend/src/i18n/language.test.ts
  - frontend/src/i18n/index.ts
  - frontend/src/i18n/locales.test.ts
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/ru/common.json
  - frontend/src/components/LanguageSwitcher.tsx
  - frontend/src/components/LanguageSwitcher.test.tsx
  - frontend/src/app/AppLayout.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02]
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a first visit with nothing stored, the UI language is Russian when the browser's primary language starts with 'ru' (e.g. ru, ru-RU, RU) and English for every other language (e.g. en-US, uk-UA, de) or when no language is reported"
    - "The header language switcher changes every visible UI string immediately, without a reload, and stores the manual choice in localStorage key `yolo-trainer.language`; after a reload the stored manual choice wins over the browser language"
    - "An unsupported stored value (e.g. 'de') is ignored and detection falls back to the browser rule"
    - "Only a manual switch writes localStorage; automatic detection never persists a value"
    - "Every translation key (including nested keys) present in any en namespace file exists in the matching ru file and vice versa"
    - "document.documentElement.lang always equals the active UI language"
  artifacts:
    - path: "frontend/src/components/LanguageSwitcher.tsx"
      provides: "manual en/ru switcher in the header"
    - path: "frontend/src/i18n/locales.test.ts"
      provides: "deep en/ru key-parity guard for every namespace (D-01)"
    - path: "frontend/src/i18n/language.ts"
      provides: "resolveInitialLanguage + changeAppLanguage"
      exports: ["resolveInitialLanguage", "changeAppLanguage", "LANGUAGE_STORAGE_KEY", "SUPPORTED_LANGUAGES"]
  key_links:
    - from: "frontend/src/components/LanguageSwitcher.tsx"
      to: "frontend/src/i18n/language.ts"
      via: "onChange -> changeAppLanguage(lng) (localStorage + i18n.changeLanguage + html lang)"
      pattern: "changeAppLanguage"
    - from: "frontend/src/app/AppLayout.tsx"
      to: "frontend/src/components/LanguageSwitcher.tsx"
      via: "rendered in the AppShell header"
      pattern: "LanguageSwitcher"
---

## Phase Goal

ROADMAP goal (verbatim): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." (Not in user-story form; see Plan 01 note.)

<objective>
Expansion slice for the bilingual UI (D-01, D-02): the user can switch between English and Russian from the header at any time, the choice sticks across reloads, first-visit language follows the browser (`ru*` -> Russian, else English), and a test guards that no translation key exists in only one language.

Purpose: the i18n foundation from the skeleton becomes user-controllable and regression-proof before more screens (Plans 08, 09) add strings.
Output: `changeAppLanguage`, `LanguageSwitcher` in the header, language tests, deep key-parity test.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-03-SUMMARY.md

<interfaces>
From Plan 01: `frontend/src/i18n/language.ts` exports `SUPPORTED_LANGUAGES = ['en', 'ru'] as const`, `type AppLanguage`, `LANGUAGE_STORAGE_KEY = 'yolo-trainer.language'`, `resolveInitialLanguage(stored: string | null, browserLanguages: readonly string[]): AppLanguage`; `frontend/src/i18n/index.ts` loads `./locales/*/*.json` with `import.meta.glob` and inits react-i18next (`defaultNS: 'common'`); `frontend/src/app/AppLayout.tsx` renders a Mantine `AppShell` header with the translated app title.
From Plan 03: Vitest harness — `renderWithProviders(ui, { route, language })` in `frontend/src/test/render.tsx`, `src/test-setup.ts` (jsdom, matchMedia/ResizeObserver polyfills, resets language to 'en' before each test).
Plan 08 runs in the same wave and adds a `project` namespace (`locales/{en,ru}/project.json`) and edits `routes.tsx`, `ProjectCard.tsx`, `api/projects.ts` — do not touch those files here; the parity test must pick up new namespace files automatically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Language resolution, persistence on manual change, html lang sync, and deep key-parity test</name>
  <files>frontend/src/i18n/language.ts, frontend/src/i18n/language.test.ts, frontend/src/i18n/index.ts, frontend/src/i18n/locales.test.ts</files>
  <read_first>
    - frontend/src/i18n/language.ts and frontend/src/i18n/index.ts (Plan 01 state)
    - frontend/src/test-setup.ts (Plan 03 — keep compatible)
    - every file under frontend/src/i18n/locales/ (current namespaces)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-01, D-02
  </read_first>
  <behavior>
    - resolveInitialLanguage(null, ['ru-RU', 'en']) == 'ru'; (null, ['RU']) == 'ru'; (null, ['en-US']) == 'en'; (null, ['uk-UA']) == 'en'; (null, []) == 'en'
    - resolveInitialLanguage('ru', ['en-US']) == 'ru' (stored manual choice wins); ('de', ['ru']) == 'ru' (unsupported stored value ignored)
    - changeAppLanguage('ru') -> localStorage['yolo-trainer.language'] == 'ru', i18n.language == 'ru', document.documentElement.lang == 'ru'
    - initializing i18n with nothing stored does not write localStorage
    - locales parity: for every namespace file found by import.meta.glob('./locales/*/*.json'), the flattened dotted key set of en equals that of ru; the failure message lists the missing keys per language
  </behavior>
  <action>
Tests first. In `language.ts` add `changeAppLanguage(lng: AppLanguage): Promise<void>` that writes `LANGUAGE_STORAGE_KEY`, calls `i18n.changeLanguage(lng)` and sets `document.documentElement.lang`. In `index.ts` set `document.documentElement.lang` after init and subscribe to i18n's `languageChanged` event to keep it in sync; detection stays `resolveInitialLanguage(localStorage.getItem(LANGUAGE_STORAGE_KEY), navigator.languages)` and never writes storage (D-02: only the manual choice is remembered). Wrap localStorage access in try/catch so a browser with storage disabled still renders (falls back to detection, choice just is not remembered). `locales.test.ts`: flatten nested JSON keys with dot paths and compare en vs ru per namespace (D-01 guard for all current and future namespaces).
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- --run</automated>
    <fails_when>non-zero exit, or language.test.ts / locales.test.ts missing from the run output, or any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- --run` exits 0 and includes `language.test.ts` and `locales.test.ts` with at least 8 passed tests between them
    - `grep -c "changeAppLanguage" frontend/src/i18n/language.ts` returns at least 1
    - `grep -c "languageChanged" frontend/src/i18n/index.ts` returns at least 1
  </acceptance_criteria>
  <done>Language detection, manual persistence and html-lang sync are tested, and a parity test fails whenever a key exists in only one language.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Header LanguageSwitcher that switches the whole UI instantly</name>
  <files>frontend/src/components/LanguageSwitcher.tsx, frontend/src/components/LanguageSwitcher.test.tsx, frontend/src/app/AppLayout.tsx, frontend/src/i18n/locales/en/common.json, frontend/src/i18n/locales/ru/common.json</files>
  <read_first>
    - frontend/src/app/AppLayout.tsx (Plan 01 header)
    - frontend/src/i18n/language.ts (Task 1)
    - frontend/src/i18n/locales/en/common.json and ru/common.json
    - frontend/src/test/render.tsx (Plan 03 helper)
  </read_first>
  <behavior>
    - the switcher shows both options labeled with their own names ("English", "Русский") and marks the active one
    - selecting "Русский" changes the header app title and the projects page title to the Russian strings without remounting the app and stores 'ru'
    - selecting "English" switches back and stores 'en'
    - the switcher has an accessible label from `common:language.label`
  </behavior>
  <action>
Create `LanguageSwitcher` (Mantine `SegmentedControl` or `Select`, compact, in the header's right side) whose options come from `SUPPORTED_LANGUAGES` with labels from new `common` keys `language.label`, `language.en` ("English" in both files) and `language.ru` ("Русский" in both files — autonyms stay the same in every UI language); `onChange` calls `changeAppLanguage`. Render it in `AppLayout`'s header next to the app title. No hardcoded user-facing strings (D-01); dark theme unchanged (D-04). Test with `renderWithProviders` rendering `AppLayout` with the projects route and a stubbed `fetch` returning `[]`.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- --run</automated>
    <fails_when>non-zero exit, or LanguageSwitcher.test.tsx reports fewer than 3 passed tests</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <human-check>End-of-phase batch (Plan 10): with the browser set to Russian, first load shows Russian; switch to English, reload, still English.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- --run` exits 0 with `LanguageSwitcher.test.tsx` contributing at least 3 passed tests, and `locales.test.ts` still passes with the new `language.*` keys
    - `grep -c "LanguageSwitcher" frontend/src/app/AppLayout.tsx` returns at least 1
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>A user can switch the entire UI between English and Russian from the header, and the choice survives reloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage -> i18n init | Browser-stored value read at startup (user- or extension-writable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | resolveInitialLanguage | low | mitigate | Stored value accepted only if it is in `SUPPORTED_LANGUAGES`; anything else ignored (tested) |
| T-07-02 | Denial of Service | localStorage access | low | mitigate | try/catch around storage access; app still renders with detected language when storage is blocked |
| T-07-03 | Tampering (XSS via translations) | i18n interpolation | low | accept | `escapeValue: false` is safe because React escapes rendered text; translations are bundled static JSON, not user-supplied |
</threat_model>

<verification>
- `npm --prefix frontend run test -- --run` green (language, parity, switcher tests)
- `npm --prefix frontend run build` green
</verification>

<success_criteria>
D-01/D-02 fully delivered: bilingual UI with browser-based default, a manual header switcher remembered in localStorage, and an automated guard against untranslated keys.
</success_criteria>

## Artifacts this phase produces (Plan 07)

- `changeAppLanguage(lng: AppLanguage): Promise<void>` in `frontend/src/i18n/language.ts`; html-lang sync on `languageChanged` in `frontend/src/i18n/index.ts`.
- Component `LanguageSwitcher` (`frontend/src/components/LanguageSwitcher.tsx`) rendered in `AppLayout`.
- i18n keys `common:language.label`, `common:language.en`, `common:language.ru`.
- Tests `language.test.ts`, `locales.test.ts` (deep en/ru parity for every namespace), `LanguageSwitcher.test.tsx`.

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-07-SUMMARY.md` when done.
</output>
