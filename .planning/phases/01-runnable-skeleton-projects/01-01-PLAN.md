---
phase: 01-runnable-skeleton-projects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .python-version
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .gitattributes
  - .dockerignore
  - .env.example
  - backend/pyproject.toml
  - backend/alembic.ini
  - backend/src/yolo_trainer_api/__init__.py
  - backend/src/yolo_trainer_api/settings.py
  - backend/src/yolo_trainer_api/db.py
  - backend/src/yolo_trainer_api/models.py
  - backend/src/yolo_trainer_api/schemas.py
  - backend/src/yolo_trainer_api/errors.py
  - backend/src/yolo_trainer_api/migrate.py
  - backend/src/yolo_trainer_api/main.py
  - backend/src/yolo_trainer_api/routers/__init__.py
  - backend/src/yolo_trainer_api/routers/projects.py
  - backend/src/yolo_trainer_api/migrations/env.py
  - backend/src/yolo_trainer_api/migrations/script.py.mako
  - backend/src/yolo_trainer_api/migrations/versions/0001_create_projects.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_projects_api.py
  - backend/tests/test_migrations.py
  - frontend/.gitignore
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/postcss.config.cjs
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/vite-env.d.ts
  - frontend/src/theme.ts
  - frontend/src/app/App.tsx
  - frontend/src/app/AppLayout.tsx
  - frontend/src/app/routes.tsx
  - frontend/src/i18n/index.ts
  - frontend/src/i18n/language.ts
  - frontend/src/i18n/locales/en/common.json
  - frontend/src/i18n/locales/ru/common.json
  - frontend/src/i18n/locales/en/projects.json
  - frontend/src/i18n/locales/ru/projects.json
  - frontend/src/api/client.ts
  - frontend/src/api/projects.ts
  - frontend/src/lib/relativeTime.ts
  - frontend/src/features/projects/ProjectsPage.tsx
  - frontend/src/features/projects/ProjectCard.tsx
  - frontend/src/features/projects/NewProjectCard.tsx
  - frontend/src/features/projects/CreateProjectModal.tsx
  - docker/Dockerfile.backend
  - docker/Dockerfile.frontend
  - docker/nginx.conf
  - docker-compose.yml
  - scripts/compose_smoke_test.sh
autonomous: true
requirements: [FOUND-01, DEPL-01, DEPL-03, PROJ-01, PROJ-02]
user_setup: []

estimate:
  tokens: 92000
  raw_tokens: 92000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Running `docker compose up --build` on the CPU-only dev machine starts `web` and `api`; http://127.0.0.1:8080/ serves the SPA and http://127.0.0.1:8080/api/health returns {\"status\":\"ok\"}"
    - "In the browser the user clicks the dashed '+ New project' card, enters a name and a task type (detect or segment), submits, and a new card with the name, a task-type badge and a relative updated time appears in the grid without a page reload"
    - "GET /api/projects returns projects sorted by updated_at descending, ties broken by id descending"
    - "POST /api/projects with a name that differs from an existing project only by letter case returns 409 with a plain-English `detail` string"
    - "With no .env file and a DATA_DIR that does not exist yet, the api creates DATA_DIR and a migrated app.db (projects table + alembic_version at head) on startup"
    - "A project created before `docker compose down` is listed again after `docker compose up`"
    - "The web port is published on 127.0.0.1 unless BIND_ADDR overrides it; the api service publishes no host port"
    - "Every user-facing UI string comes from en/ru i18n namespace files; the UI renders in the Mantine dark color scheme only"
    - "`uv run pytest backend/tests -x` passes on the dev machine inside the uv-managed Python 3.12 venv"
  prohibitions:
    - statement: "MUST NOT publish any service port on a non-loopback interface by default: only `web` publishes a host port and it binds to ${BIND_ADDR:-127.0.0.1}; `api` has no `ports:` entry"
      status: resolved
      verification: test
    - statement: "MUST NOT load UI assets from or send project data to any third-party host: the SPA uses only relative `/api` URLs and same-origin assets, enforced by a `default-src 'self'` Content-Security-Policy on the web container"
      status: resolved
      verification: test
  artifacts:
    - path: "pyproject.toml"
      provides: "uv workspace root (members = backend), dev dependency group, non-package root project"
      contains: "[tool.uv.workspace]"
    - path: "backend/src/yolo_trainer_api/main.py"
      provides: "create_app factory, lifespan running Alembic upgrade head, module-level app"
      exports: ["create_app", "app"]
    - path: "backend/src/yolo_trainer_api/models.py"
      provides: "Project model with normalized_name unique index"
      contains: "uq_projects_normalized_name"
    - path: "backend/src/yolo_trainer_api/routers/projects.py"
      provides: "GET/POST /api/projects"
      exports: ["router"]
    - path: "backend/src/yolo_trainer_api/migrations/versions/0001_create_projects.py"
      provides: "initial schema migration"
      contains: "revision = \"0001\""
    - path: "frontend/src/features/projects/CreateProjectModal.tsx"
      provides: "create form wired to POST /api/projects"
    - path: "docker/nginx.conf"
      provides: "SPA fallback + unbuffered /api reverse proxy + security headers"
      contains: "proxy_buffering off"
    - path: "docker-compose.yml"
      provides: "web + api services, ./data bind mount, loopback port binding"
      contains: "${BIND_ADDR:-127.0.0.1}"
    - path: "scripts/compose_smoke_test.sh"
      provides: "end-to-end compose acceptance check (create -> down/up -> still listed)"
      contains: "SMOKE OK"
  key_links:
    - from: "frontend/src/features/projects/CreateProjectModal.tsx"
      to: "/api/projects"
      via: "useCreateProject mutation in frontend/src/api/projects.ts -> apiRequest POST"
      pattern: "useCreateProject"
    - from: "docker/nginx.conf"
      to: "api:8000"
      via: "location /api/ proxy_pass"
      pattern: "proxy_pass http://api:8000"
    - from: "backend/src/yolo_trainer_api/main.py"
      to: "backend/src/yolo_trainer_api/migrate.py"
      via: "lifespan awaits asyncio.to_thread(run_migrations, settings) before yield"
      pattern: "run_migrations"
    - from: "docker-compose.yml"
      to: "/data"
      via: "bind mount ${DATA_DIR:-./data}:/data on api"
      pattern: "DATA_DIR:-./data"
    - from: "frontend/vite.config.ts"
      to: "http://127.0.0.1:8000"
      via: "server.proxy['/api'] for native dev (D-19)"
      pattern: "'/api'"
---

## Phase Goal

ROADMAP goal (verbatim, not yet in user-story form): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." Run `/gsd-mvp-phase 1` if a formal "As a / I want to / so that" story is wanted; none is invented here.

<objective>
Walking skeleton (WALKING_SKELETON=true, see `SKELETON.md`): the thinnest production-quality path through every layer the phase touches. A user runs `docker compose up --build`, opens http://127.0.0.1:8080, creates a project, sees it in the grid, and it survives `down`/`up`.

Purpose: prove the architecture end-to-end (SPA -> nginx -> FastAPI -> Alembic-migrated SQLite on the `./data` bind mount) before any expansion work, so later plans only add slices on top of a proven stack.
Output: uv workspace + `yolo_trainer_api` package with create/list endpoints, Vite/React/Mantine/i18n SPA with the project grid and create modal, `web`+`api` compose stack, compose smoke test, pytest harness with API and migration tests.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-projects/01-PATTERNS.md
@.planning/phases/01-runnable-skeleton-projects/SKELETON.md

<interfaces>
API contract created by this plan and consumed by Plans 02, 03, 07, 08, 09 (do not deviate):

- `GET /api/health` -> 200 `{"status": "ok"}`
- `GET /api/projects` -> 200 `Project[]`, ordered `updated_at DESC, id DESC`
- `POST /api/projects` body `{"name": str, "task_type": "detect"|"segment", "description": str|null}` -> 201 `Project`; 409 on case-insensitive duplicate name; 422 on invalid input
- `Project` JSON: `{"id": int, "name": str, "task_type": "detect"|"segment", "description": str|null, "created_at": "<UTC ISO-8601 ending in Z>", "updated_at": "<same>"}`
- Every error body is `{"detail": "<plain English sentence>"}` (a string, never a list) — D-05
- Later plans add `GET/PATCH/DELETE /api/projects/{project_id}` (Plans 08, 09)

Python symbols created: `yolo_trainer_api.settings.Settings`, `yolo_trainer_api.main.create_app(settings: Settings | None = None) -> FastAPI`, `yolo_trainer_api.main.app`, `yolo_trainer_api.migrate.run_migrations(settings: Settings) -> None`, `yolo_trainer_api.db.Base`, `yolo_trainer_api.db.create_engine_for(settings) -> AsyncEngine`, `yolo_trainer_api.db.get_session` (FastAPI dependency), `yolo_trainer_api.models.Project`, `yolo_trainer_api.models.normalize_project_name(name: str) -> str`, `yolo_trainer_api.schemas.{TaskType, ProjectCreate, ProjectRead}`, `yolo_trainer_api.errors.register_error_handlers(app)`, `yolo_trainer_api.routers.projects.router`.

Frontend symbols created: `apiRequest<T>(path, init?)`, `ApiError` (`status: number`, `message` = API detail), `Project`, `TaskType`, `projectKeys`, `useProjects()`, `useCreateProject()`, `formatRelativeTime(iso, locale, now?)`, `resolveInitialLanguage(stored, browserLanguages)`, `LANGUAGE_STORAGE_KEY = 'yolo-trainer.language'`, `SUPPORTED_LANGUAGES = ['en', 'ru']`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "create a project and see it listed" through the compose stack — one path only</name>
  <precondition>Docker Desktop is running: `docker info` exits 0 and `docker compose version` prints v2+ (external setup, not created by this plan)</precondition>
  <reversibility rating="costly">Implements D-01 (i18n from day one), D-08 (normalized_name unique index in migration 0001) and D-13 (./data bind-mount layout): undoing needs string extraction, an Alembic migration, or moving users' data.</reversibility>
  <files>.python-version, pyproject.toml, uv.lock, .gitignore, .gitattributes, .dockerignore, .env.example, backend/pyproject.toml, backend/alembic.ini, backend/src/yolo_trainer_api/{__init__,settings,db,models,schemas,errors,migrate,main}.py, backend/src/yolo_trainer_api/routers/{__init__,projects}.py, backend/src/yolo_trainer_api/migrations/{env.py,script.py.mako,versions/0001_create_projects.py}, frontend/{.gitignore,package.json,package-lock.json,tsconfig.json,vite.config.ts,postcss.config.cjs,index.html}, frontend/src/{main.tsx,vite-env.d.ts,theme.ts}, frontend/src/app/{App,AppLayout,routes}.tsx, frontend/src/i18n/{index,language}.ts, frontend/src/i18n/locales/{en,ru}/{common,projects}.json, frontend/src/api/{client,projects}.ts, frontend/src/lib/relativeTime.ts, frontend/src/features/projects/{ProjectsPage,ProjectCard,NewProjectCard,CreateProjectModal}.tsx, docker/{Dockerfile.backend,Dockerfile.frontend,nginx.conf}, docker-compose.yml, scripts/compose_smoke_test.sh</files>
  <read_first>
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md (D-01..D-22 are all exercised by this path)
    - .planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md sections "Architecture Patterns" (Patterns 1-6), "Code Examples", "Common Pitfalls" 2-4, "Package Legitimacy Audit"
    - .planning/phases/01-runnable-skeleton-projects/SKELETON.md (architectural contract this task makes real)
    - .gitignore (current rules; keep every existing rule)
    - train_yolo.py and finetune_yolo.py (must NOT be modified by this plan; only read to confirm they stay at the repo root)
  </read_first>
  <action>
Build ONE thin path: create + list projects, through SPA -> nginx -> FastAPI -> SQLite. No detail/rename/delete, no worker service, no Vitest here (later plans). Everything production-quality: real validation, real error handling on this path.

Toolchain (D-21, D-22, RESEARCH Pattern 1). If `uv --version` fails, install uv first (`python -m pip install --user uv` pinned to the latest version shown by `python -m pip index versions uv`, or the official Astral installer) and record the uv version in the SUMMARY. Create `.python-version` containing `3.12`. Root `pyproject.toml`: `[project] name = "yolo-trainer"`, `version = "0.1.0"`, `requires-python = ">=3.12,<3.13"`, `dependencies = ["yolo-trainer-backend"]`; `[dependency-groups] dev = ["pytest>=9.1", "httpx>=0.28", "ruff>=0.16"]`; `[tool.uv] package = false`; `[tool.uv.workspace] members = ["backend"]`; `[tool.uv.sources] yolo-trainer-backend = { workspace = true }`. `backend/pyproject.toml`: `[project] name = "yolo-trainer-backend"`, same `requires-python`, dependencies `fastapi>=0.141.1`, `uvicorn>=0.53.0`, `sqlalchemy>=2.0.54,<2.1`, `alembic>=1.20.0`, `aiosqlite>=0.22.1`, `pydantic>=2.13`, `pydantic-settings>=2.15`; hatchling build backend with `[tool.hatch.build.targets.wheel] packages = ["src/yolo_trainer_api"]`; `[tool.pytest.ini_options] testpaths = ["tests"]`, `addopts = "-ra"`; `[tool.ruff] line-length = 100`, `target-version = "py312"`, `[tool.ruff.lint] select = ["E", "F", "I", "UP", "B"]` (D-24: English comments + type hints in all new backend code). Run `uv lock` then `uv sync`; commit `uv.lock`. Install ONLY packages named in this plan or in RESEARCH's audit table (all verified legitimate).

Backend package `backend/src/yolo_trainer_api/`:
- `settings.py`: `Settings(BaseSettings)` with `data_dir: Path = Path("data")` (env `DATA_DIR`), properties `db_path` (`data_dir / "app.db"`), `async_database_url` (`sqlite+aiosqlite:///` + absolute POSIX path), `sync_database_url` (`sqlite:///` + same). No `.env` file loading (compose `.env` holds host-side values; the container gets `DATA_DIR=/data` explicitly).
- `db.py`: `class Base(DeclarativeBase)`; `create_engine_for(settings) -> AsyncEngine` registering a `connect` listener on `engine.sync_engine` that runs `PRAGMA journal_mode=WAL`, `PRAGMA synchronous=NORMAL`, `PRAGMA busy_timeout=30000`, `PRAGMA foreign_keys=ON` (RESEARCH Pattern 3); `create_sessionmaker(engine)` (`expire_on_commit=False`); dependency `get_session(request)` yielding an `AsyncSession` from `request.app.state.sessionmaker`.
- `models.py`: `normalize_project_name(name)` = `unicodedata.normalize("NFKC", name).strip().casefold()`; `utcnow()` = `datetime.now(UTC)`; `Project` on table `projects` (D-07: required name, required task type, optional description, automatic timestamps): `id` Integer PK, `name` String(100) not null, `normalized_name` Text not null, `task_type` String(16) not null with CheckConstraint `task_type IN ('detect', 'segment')` named `ck_projects_task_type`, `description` Text nullable, `created_at`/`updated_at` `DateTime(timezone=True)` not null with Python-side `default=utcnow` and `updated_at` `onupdate=utcnow`; unique index `uq_projects_normalized_name` on `normalized_name` (D-08, Postgres-portable; no SQLite-only collation); method `set_name(raw)` storing `unicodedata.normalize("NFC", raw).strip()` in `name` and `normalize_project_name(raw)` in `normalized_name`. Task type is only ever set at creation (D-09).
- `schemas.py`: `TaskType = Literal["detect", "segment"]`; `ProjectCreate` (`model_config = ConfigDict(extra="forbid")`, `name` stripped with min length 1 / max length 100 code points, `task_type: TaskType`, `description: str | None` max 2000); `ProjectRead` (`from_attributes=True`; a `field_serializer` for `created_at`/`updated_at` that attaches UTC to naive values so JSON renders with a trailing `Z`).
- `errors.py`: `register_error_handlers(app)` adding a `RequestValidationError` handler returning 422 with `{"detail": "<field>: <message>"}` pieces joined by `"; "` (drop the leading `body` location element and a leading `"Value error, "` prefix) — detail is always one plain-English string (D-05).
- `routers/projects.py`: `router = APIRouter(prefix="/api/projects", tags=["projects"])`; `GET ""` returns `ProjectRead` list ordered `Project.updated_at.desc(), Project.id.desc()` (D-12); `POST ""` status 201 builds a `Project`, calls `set_name`, commits, and on `IntegrityError` rolls back and raises `HTTPException(409, detail=f'A project named "{name}" already exists.')` (RESEARCH Pattern 2 — no check-then-insert).
- `migrate.py`: `run_migrations(settings)` builds `alembic.config.Config()` programmatically with `script_location` = the package's `migrations` directory (resolved from `__file__`) and `sqlalchemy.url` = `settings.sync_database_url`, then `command.upgrade(cfg, "head")`.
- `migrations/env.py` (sync engine from the configured URL, `target_metadata = Base.metadata`, `render_as_batch=True`, no `fileConfig` when there is no ini file), `migrations/script.py.mako` (standard Alembic template), `migrations/versions/0001_create_projects.py` (`revision = "0001"`, `down_revision = None`, creates `projects` with the constraint and unique index above; downgrade drops them). `backend/alembic.ini` sets `script_location = %(here)s/src/yolo_trainer_api/migrations` for CLI use.
- `main.py`: `create_app(settings=None)`; lifespan: `settings.data_dir.mkdir(parents=True, exist_ok=True)`, `await asyncio.to_thread(run_migrations, settings)`, create engine + sessionmaker on `app.state`, `yield`, `await engine.dispose()` (D-20; RESEARCH Pattern 6; one api replica so no migration race). Register `GET /api/health` -> `{"status": "ok"}`, `register_error_handlers`, include the projects router. Module-level `app = create_app()`.

Frontend `frontend/` (D-01 i18n, D-02 language default, D-03 Mantine component library, D-04 dark only, D-05 API text shown as-is, D-06 card grid, D-19 Vite dev proxy; npm, no pnpm). `npm install react react-dom @mantine/core @mantine/hooks @tanstack/react-query react-router-dom i18next react-i18next` and `npm install -D vite @vitejs/plugin-react typescript @types/react @types/react-dom postcss postcss-preset-mantine postcss-simple-vars` (only the scoped `@mantine/*` packages exist; the unscoped name is not a real package). Scripts: `dev` = `vite`, `build` = `tsc --noEmit && vite build`, `preview` = `vite preview`. `vite.config.ts`: react plugin; `server.proxy` and `preview.proxy` map `'/api'` to `http://127.0.0.1:8000` (native dev, D-19). `postcss.config.cjs` per Mantine docs (preset-mantine + simple-vars breakpoints). `index.html`: `<html lang="en" data-mantine-color-scheme="dark">`, `#root`, module script; no external fonts, CDNs or analytics.
- `src/theme.ts` `createTheme` with a system font stack; `src/app/App.tsx`: `MantineProvider theme forceColorScheme="dark"` (D-04, dark only — no toggle code path), `QueryClientProvider`, `BrowserRouter`; sets `document.title` from `t('common:app.title')`. `src/app/AppLayout.tsx`: Mantine `AppShell` with a header showing the translated app title and an `<Outlet />`. `src/app/routes.tsx`: `/` redirects to `/projects`; `/projects` renders `ProjectsPage`.
- `src/i18n/language.ts`: `SUPPORTED_LANGUAGES = ['en', 'ru'] as const`, `type AppLanguage`, `LANGUAGE_STORAGE_KEY = 'yolo-trainer.language'`, `resolveInitialLanguage(stored, browserLanguages)` -> stored value if supported, else `'ru'` when `browserLanguages[0]` lowercased starts with `ru`, else `'en'` (D-02). `src/i18n/index.ts`: load every `./locales/*/*.json` via `import.meta.glob(..., { eager: true, import: 'default' })` into `resources[lng][ns]`, init react-i18next with `lng = resolveInitialLanguage(localStorage.getItem(LANGUAGE_STORAGE_KEY), navigator.languages)`, `fallbackLng: 'en'`, `supportedLngs: ['en', 'ru']`, `defaultNS: 'common'`, `interpolation.escapeValue: false`. Namespaces `common` (app title, generic error title, loading) and `projects` (page title, new-project card, modal labels, task-type labels, submit/cancel, empty-state hint) with identical key sets in `en` and `ru` — no user-facing string literal in any component (D-01).
- `src/api/client.ts`: `class ApiError extends Error` with `status`; `apiRequest<T>(path, init?)` fetching `'/api' + path` with JSON headers when a body is present, returning `undefined` for 204, and on non-2xx throwing `ApiError(detail, status)` where `detail` is the response JSON `detail` string (fallback `statusText`). `src/api/projects.ts`: `TaskType`, `Project`, `ProjectCreateInput`, `projectKeys = { all: ['projects'] }`, `useProjects()`, `useCreateProject()` invalidating `projectKeys.all` on success.
- `src/lib/relativeTime.ts`: `formatRelativeTime(iso, locale, now = new Date())` using `Intl.RelativeTimeFormat(locale, { numeric: 'auto' })`, picking seconds/minutes/hours/days/months/years by elapsed time and clamping future timestamps to 0 ("now").
- `src/features/projects/ProjectsPage.tsx`: title, `SimpleGrid` of `ProjectCard` plus a dashed `NewProjectCard` (D-06) that opens `CreateProjectModal`; loading state; error state showing the translated error title plus `ApiError.message`; when the list is empty, show the new-project card plus the empty-state hint. `ProjectCard.tsx`: name, `Badge` with the task type, `formatRelativeTime(project.updated_at, i18n.language)`; leave the card body structured (Card sections) so Phase 2 can add a thumbnail and counts. `CreateProjectModal.tsx`: Mantine `Modal` with name `TextInput` (required), task type `SegmentedControl` (`detect` | `segment`, required, default `detect`), optional description `Textarea`; submit calls `useCreateProject`; on `ApiError` show the API message verbatim under the translated error title (D-05); on success close and reset.

Compose stack (D-13..D-17, RESEARCH Pattern 5, Code Examples):
- `docker/Dockerfile.backend`: stage `base` from `python:3.12-slim` with `PYTHONUNBUFFERED=1`, `UV_COMPILE_BYTECODE=1`, `UV_LINK_MODE=copy`, `UV_PYTHON_DOWNLOADS=never`; install uv via pip pinned to the same uv version that produced `uv.lock` (`ARG UV_VERSION`); `WORKDIR /app`; copy `pyproject.toml`, `uv.lock`, `.python-version`, `backend/pyproject.toml`, `backend/src`. Stage `api` from `base`: `uv sync --frozen --no-dev --package yolo-trainer-backend` (no torch — D-17; RESEARCH Pitfall 4), `ENV DATA_DIR=/data`, `EXPOSE 8000`, `CMD ["/app/.venv/bin/uvicorn", "yolo_trainer_api.main:app", "--host", "0.0.0.0", "--port", "8000"]`. The worker stage is added by Plan 06.
- `docker/Dockerfile.frontend`: `node:24-alpine` build stage (`npm ci`, `npm run build`) then `nginx:alpine` copying `dist/` to `/usr/share/nginx/html` and `docker/nginx.conf` to `/etc/nginx/conf.d/default.conf`.
- `docker/nginx.conf`: `listen 80`; `server_tokens off`; `client_max_body_size 1m` (this phase only accepts small JSON bodies); server-level `add_header ... always` for `X-Content-Type-Options nosniff`, `X-Frame-Options DENY`, `Referrer-Policy same-origin`, and `Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'"` (no `add_header` inside location blocks); `location /api/` -> `proxy_pass http://api:8000;` with `proxy_http_version 1.1`, `proxy_set_header Connection ""`, `proxy_set_header Host $host`, `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for`, `proxy_buffering off`, `proxy_cache off`, `proxy_read_timeout 3600s` (SSE-ready, D-15); `location /` with `root /usr/share/nginx/html` and `try_files $uri $uri/ /index.html` (SPA fallback).
- `docker-compose.yml`: top-level `name: yolo-trainer`; service `api` (build context `.`, dockerfile `docker/Dockerfile.backend`, target `api`, `image: yolo-trainer-api:local`, volume `${DATA_DIR:-./data}:/data`, env `DATA_DIR: /data`, healthcheck running `python -c` with `urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3)`, `restart: unless-stopped`, NO `ports:`); service `web` (dockerfile `docker/Dockerfile.frontend`, `image: yolo-trainer-web:local`, `ports: ["${BIND_ADDR:-127.0.0.1}:${PORT:-8080}:80"]`, `depends_on: api: condition: service_healthy`, healthcheck `wget -q -O /dev/null http://127.0.0.1/api/health`, `restart: unless-stopped`). Images are built locally only (D-16).
- `.env.example` documents `DATA_DIR=./data`, `BIND_ADDR=127.0.0.1`, `PORT=8080` with a comment that setting `BIND_ADDR=0.0.0.0` exposes an app with no authentication to the whole network (D-14).
- `.dockerignore` excludes `.git`, `**/node_modules`, `**/.venv`, `**/__pycache__`, `data`, `.smoke-data`, `runs`, `trained_models`, `*.pt`, `example_ready_dataset`, `frontend/dist`, `.planning`, `.idea`, `.claude`, `spikes`.
- `.gitattributes`: `* text=auto` and `*.sh text eol=lf` (bash scripts must keep LF on Windows checkouts).
- Root `.gitignore`: append `data/`, `.smoke-data/`, `.env`, `.pytest_cache/`, `.ruff_cache/` (keep all existing rules). `frontend/.gitignore`: `node_modules/`, `dist/`, `coverage/`, `*.tsbuildinfo`.
- `scripts/compose_smoke_test.sh` (bash, `set -euo pipefail`, LF endings; no jq dependency): export `COMPOSE_PROJECT_NAME=yolo-trainer-smoke`, `DATA_DIR=./.smoke-data`, `PORT=18080`, `BIND_ADDR=127.0.0.1`; start from an empty `.smoke-data` (remove leftovers via a throwaway `docker compose run --rm --no-deps --entrypoint sh api -c` that empties `/data`, then remove the directory); `docker compose up -d --build --wait --wait-timeout 600`; assert `docker compose port web 80` starts with `127.0.0.1:`; assert `curl -fsS http://127.0.0.1:18080/` contains `id="root"`; assert `/api/health` returns `"status":"ok"`; POST a project with a unique ASCII name and task type `detect` expecting HTTP 201; assert the list response contains `"name":"<that name>"`; assert `.smoke-data/app.db` exists on the host; `docker compose down`; `docker compose up -d --wait`; assert the name is still listed; cleanup via `trap` (compose down + container-based emptying + remove `.smoke-data`) unless `KEEP_SMOKE_DATA=1`; print `SMOKE OK` as the last line on success.

Run the verify commands; if the SPA does not render under the CSP, fix the policy (keep `default-src 'self'`) rather than removing it.
  </action>
  <verify>
    <automated>bash scripts/compose_smoke_test.sh</automated>
    <fails_when>non-zero exit, or the last output line is not `SMOKE OK`</fails_when>
    <automated>uv run ruff check backend && uv run ruff format --check backend</automated>
    <fails_when>non-zero exit, or output lists any file under backend/ as "Would reformat" or with a lint code</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit, or a TypeScript error line ("error TS") in the output</fails_when>
    <human-check>End-of-phase batch (Plan 10): open http://127.0.0.1:8080 after `docker compose up --build`, create a project via the dashed card, confirm the dark theme and that the new card shows name, badge and relative time.</human-check>
  </verify>
  <acceptance_criteria>
    - `bash scripts/compose_smoke_test.sh` exits 0 and prints `SMOKE OK` (proves SPA served, /api proxied, create 201, list contains it, host `app.db` exists, survives down/up, web bound to 127.0.0.1)
    - `grep -n "ports:" -A1 docker-compose.yml` shows exactly one ports mapping, under `web`, containing `${BIND_ADDR:-127.0.0.1}:${PORT:-8080}:80`
    - `grep -c "proxy_buffering off" docker/nginx.conf` returns 1 and `grep -c "try_files \$uri \$uri/ /index.html" docker/nginx.conf` returns 1
    - `grep -c "default-src 'self'" docker/nginx.conf` returns 1
    - `grep -c "uq_projects_normalized_name" backend/src/yolo_trainer_api/migrations/versions/0001_create_projects.py` returns at least 1
    - `grep -c 'requires-python = ">=3.12,<3.13"' pyproject.toml backend/pyproject.toml` reports 1 for each file
    - `grep -c "forceColorScheme" frontend/src/app/App.tsx` returns 1
    - `grep -rEn "https?://" frontend/src frontend/index.html` prints nothing (no third-party hosts in the SPA)
    - `uv run ruff check backend` and `uv run ruff format --check backend` exit 0; `npm --prefix frontend run build` exits 0
    - `git diff --stat "$PLAN_BASE" -- train_yolo.py finetune_yolo.py gpu_test.py config/config.ini` prints nothing, where PLAN_BASE is the commit SHA recorded before Task 1 started (legacy scripts untouched by this plan)
  </acceptance_criteria>
  <done>A user can run `docker compose up --build`, open http://127.0.0.1:8080, create a project and see it listed after a restart; the smoke test proves it automatically and the path is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pytest harness proving create/list, case-insensitive 409, ordering, and migrations on a fresh DATA_DIR</name>
  <files>backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_projects_api.py, backend/tests/test_migrations.py</files>
  <read_first>
    - backend/src/yolo_trainer_api/main.py, settings.py, models.py, routers/projects.py, migrate.py (created in Task 1 — test against their real behavior)
    - .planning/phases/01-runnable-skeleton-projects/01-VALIDATION.md (quick command `uv run pytest backend/tests -x`, Wave 0 conftest requirement)
    - .planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md "Validation Architecture"
  </read_first>
  <behavior>
    - test_health_ok: GET /api/health -> 200 {"status": "ok"}
    - test_create_and_list_round_trip: POST {"name": "Cars", "task_type": "detect"} -> 201 with id, name "Cars", task_type "detect", description null, created_at/updated_at ending in "Z"; GET /api/projects contains it
    - test_duplicate_name_is_case_insensitive: after "Cars", POST " cars " -> 409 and detail is a str mentioning "already exists"; list length stays 1
    - test_list_sorted_by_updated_at_desc: create A then B -> list order is [B, A]
    - test_invalid_task_type_rejected: task_type "obb" -> 422 with a str detail
    - test_startup_creates_data_dir_and_migrates: DATA_DIR = tmp_path/"fresh"/"data" (not existing) -> after app startup the dir and app.db exist, sqlite_master has table "projects", alembic_version.version_num == "0001"
    - test_data_persists_across_app_restart: create in one app instance, close it, start a second app on the same DATA_DIR -> project still listed
  </behavior>
  <action>
Write the tests first (RED), then fix any Task 1 defect they expose (GREEN). `conftest.py`: fixture `settings(tmp_path)` returning `Settings(data_dir=tmp_path / "data")`; fixture `client(settings)` that enters `TestClient(create_app(settings))` as a context manager (so the lifespan runs migrations) and yields it; helper `make_client(settings)` for the restart test. Tests use only the public HTTP API plus `sqlite3` from the stdlib to inspect the migrated file (read-only). Keep tests independent (each gets its own `tmp_path`). English comments and type hints (D-24). Do not mock the database — the real SQLite file and real Alembic migration run in every test (D-20, D-27).
  </action>
  <verify>
    <automated>uv run pytest backend/tests -x</automated>
    <fails_when>non-zero exit, or the summary line shows "no tests ran" or fewer than 7 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests -x -q` exits 0 and its summary reports at least 7 passed
    - `grep -c "def test_" backend/tests/test_projects_api.py` returns at least 5 and `grep -c "def test_" backend/tests/test_migrations.py` returns at least 2
    - `grep -c "TestClient(create_app" backend/tests/conftest.py` returns at least 1
    - `uv run ruff check backend` exits 0
  </acceptance_criteria>
  <done>The skeleton's API/DB/migration behavior is pinned by passing pytest tests that run against a real migrated SQLite file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> web (nginx :8080) | Anyone who can reach the published port; no authentication in v1 |
| web -> api (compose network) | Proxied JSON requests; untrusted body content (project name/description) |
| api -> SQLite on host bind mount | Persistent user data crossing the container/host filesystem boundary |
| developer machine -> npm/PyPI | Third-party packages installed into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure / Elevation of Privilege | docker-compose.yml `web.ports` | high | mitigate | Publish only `web`, bound to `${BIND_ADDR:-127.0.0.1}`; `api` has no `ports:`; smoke test asserts `docker compose port web 80` starts with `127.0.0.1:`; `.env.example` warns about `0.0.0.0` (D-14) |
| T-01-02 | Tampering | routers/projects.py | medium | mitigate | SQLAlchemy ORM parameterized statements only; no raw SQL string formatting |
| T-01-03 | Denial of Service | schemas.py, nginx.conf | low | mitigate | Pydantic `max_length` (name 100, description 2000); `extra="forbid"`; nginx `client_max_body_size 1m` |
| T-01-04 | Tampering (stored XSS) | ProjectCard / CreateProjectModal | medium | mitigate | React text rendering only (no `dangerouslySetInnerHTML`); CSP `script-src 'self'` blocks injected script |
| T-01-05 | Information Disclosure | index.html / nginx CSP | medium | mitigate | No CDN/font/analytics hosts; CSP `default-src 'self'`, `connect-src 'self'`; acceptance grep finds no `http(s)://` in `frontend/src` |
| T-01-06 | Tampering (integrity) | SQLite WAL on Docker Desktop bind mount | medium | mitigate | WAL + `synchronous=NORMAL` + `busy_timeout`; smoke test exercises write -> down/up; further mitigations in Plan 02 |
| T-01-07 | Elevation of Privilege | api container runs as root | low | accept | Local single-operator tool with no host ports on `api`; revisit when auth or uploads land |
| T-01-SC | Tampering | npm / pip / uv installs | high | mitigate | Only packages in RESEARCH's audit table plus planner-verified extras (@vitejs/plugin-react, typescript, postcss, postcss-preset-mantine, postcss-simple-vars, @types/react, @types/react-dom, pydantic-settings, httpx, hatchling — registry + repo + download counts checked 2026-09-24); lockfiles committed; Docker uses `npm ci` and `uv sync --frozen`; the unscoped `mantine` name is never installed |
</threat_model>

<verification>
- `bash scripts/compose_smoke_test.sh` -> `SMOKE OK`
- `uv run pytest backend/tests -x` green
- `npm --prefix frontend run build` green; `uv run ruff check backend` green
</verification>

<success_criteria>
The walking skeleton runs: `docker compose up --build` serves the SPA on 127.0.0.1:8080, a project created through the UI/API persists across `down`/`up`, migrations apply on startup, and the backend test harness passes.
</success_criteria>

## Artifacts this phase produces (Plan 01)

- Files: every path in `files_modified` above.
- Python: `Settings` (fields `data_dir`; properties `db_path`, `async_database_url`, `sync_database_url`), `create_app`, `app`, `run_migrations`, `Base`, `create_engine_for`, `create_sessionmaker`, `get_session`, `Project` (+ `set_name`), `normalize_project_name`, `utcnow`, `TaskType`, `ProjectCreate`, `ProjectRead`, `register_error_handlers`, `router` (projects), Alembic revision `0001`, constraint `ck_projects_task_type`, index `uq_projects_normalized_name`.
- HTTP: `GET /api/health`, `GET /api/projects`, `POST /api/projects`.
- Frontend: `App`, `AppLayout`, `routes`, `theme`, `ApiError`, `apiRequest`, `Project`, `TaskType`, `ProjectCreateInput`, `projectKeys`, `useProjects`, `useCreateProject`, `formatRelativeTime`, `resolveInitialLanguage`, `LANGUAGE_STORAGE_KEY`, `SUPPORTED_LANGUAGES`, `AppLanguage`, components `ProjectsPage`, `ProjectCard`, `NewProjectCard`, `CreateProjectModal`; i18n namespaces `common`, `projects`.
- Docker/compose: stages `base`, `api`; services `api`, `web`; images `yolo-trainer-api:local`, `yolo-trainer-web:local`; env vars `DATA_DIR`, `BIND_ADDR`, `PORT`.
- Scripts: `scripts/compose_smoke_test.sh` (env `KEEP_SMOKE_DATA`, prints `SMOKE OK`).
- Tests: `backend/tests/conftest.py` fixtures `settings`, `client`, helper `make_client`.

## Edge coverage (spec-less probe fallback, resolved here)

- FOUND-01 / empty: resolved (explicit) -> truth "With no .env file and a DATA_DIR that does not exist yet ..." (tested by `test_startup_creates_data_dir_and_migrates` and the smoke test's fresh `.smoke-data`).

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-01-SUMMARY.md` when done (record the uv version used and the exact versions resolved in uv.lock / package-lock.json).
</output>
