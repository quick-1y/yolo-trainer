---
phase: 01-runnable-skeleton-projects
plan: 10
type: execute
wave: 5
depends_on: [01-02, 01-05, 01-06, 01-07, 01-09]
files_modified:
  - README.md
  - README.ru.md
  - scripts/run_full_suite.sh
  - scripts/fresh_clone_check.sh
autonomous: true
requirements: [FOUND-01, DEPL-01, DEPL-03]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A fresh `git clone` of the repository followed only by the README's developer steps (`uv sync --locked`, `uv run pytest backend/tests`, `npm --prefix frontend ci`, `npm --prefix frontend run test -- --run`) succeeds with no unstated step"
    - "README.md documents both run paths: Docker (`docker compose up --build`, open http://127.0.0.1:8080, `docker compose down`) and native development (`uv run uvicorn ... --reload` + `npm --prefix frontend run dev` with the Vite /api proxy)"
    - "README.md lists every .env variable (DATA_DIR, BIND_ADDR, PORT, ALLOWED_HOSTS, SQLITE_JOURNAL_MODE) with defaults, and warns prominently that the app has no authentication, so BIND_ADDR=0.0.0.0 gives everyone on the network full access"
    - "README.md explains that app data lives in ./data (or DATA_DIR), survives down/up and rebuilds, how to back it up, the Docker Desktop (Windows/macOS) SQLite WAL caveat with the SQLITE_JOURNAL_MODE=DELETE escape hatch, and that native Linux Docker is unaffected"
    - "README.md documents the legacy CLI scripts (run via `uv run python train_yolo.py`), that `.pt` files are pickle-based and must only come from trusted sources, and the known limitations (gpu_test.py needs a GPU until Phase 12; train_yolo.py's `Config/Config.ini` path on case-sensitive filesystems)"
    - "README.ru.md is a Russian translation with the same sections and the same commands"
    - "`bash scripts/run_full_suite.sh` runs the full phase suite (pytest, ruff check + format, Vitest, frontend build, compose smoke test, CLI git-clean check) and ends with `ALL CHECKS OK`"
    - "`bash scripts/fresh_clone_check.sh` clones the committed HEAD into a temporary directory, runs the README developer steps there and ends with `FRESH CLONE OK`"
  artifacts:
    - path: "README.md"
      provides: "English setup, run, configuration, security and troubleshooting guide"
      contains: "docker compose up --build"
    - path: "README.ru.md"
      provides: "Russian translation of README.md"
      contains: "docker compose up --build"
    - path: "scripts/run_full_suite.sh"
      provides: "one-command full phase suite"
      contains: "ALL CHECKS OK"
    - path: "scripts/fresh_clone_check.sh"
      provides: "fresh-clone rehearsal of the README developer steps (FOUND-01)"
      contains: "FRESH CLONE OK"
  key_links:
    - from: "README.md"
      to: "README.ru.md"
      via: "language link at the top of each README"
      pattern: "README.ru.md"
    - from: "README.md"
      to: ".env.example"
      via: "configuration section mirrors the variables documented in .env.example"
      pattern: "ALLOWED_HOSTS"
---

## Phase Goal

ROADMAP goal (verbatim): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." (Not in user-story form; see Plan 01 note.)

<objective>
Close the phase: English README (D-26) plus Russian translation that take a new user and a new developer from a fresh clone to a running, tested system with no guessing (FOUND-01), including the D-14 no-auth warning and the D-13/RESEARCH Pitfall 1 persistence caveats; then run the whole phase suite once and hand the UI flows to the end-of-phase human check.

Purpose: success criteria 1, 4 and 5 are only true if someone other than the implementer can follow the documentation.
Output: `README.md`, `README.ru.md`, a green full-suite run, the human-check list.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-projects/01-VALIDATION.md
@.planning/phases/01-runnable-skeleton-projects/SKELETON.md
@.planning/phases/01-runnable-skeleton-projects/01-02-SUMMARY.md
@.planning/phases/01-runnable-skeleton-projects/01-04-SUMMARY.md
@.planning/phases/01-runnable-skeleton-projects/01-05-SUMMARY.md
@.planning/phases/01-runnable-skeleton-projects/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: English README covering Docker use, developer setup, configuration, security and troubleshooting</name>
  <files>README.md</files>
  <read_first>
    - README.md (currently a single title line)
    - .env.example, docker-compose.yml, pyproject.toml, backend/pyproject.toml, frontend/package.json, scripts/compose_smoke_test.sh, scripts/check_cli_run_git_clean.py (document what actually exists — every command in the README must be one that works in this repo)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-13, D-14, D-19, D-22, D-26
    - .planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md "Common Pitfalls" 1-3 and "Environment Availability"
    - .planning/phases/01-runnable-skeleton-projects/01-04-SUMMARY.md and 01-05-SUMMARY.md (known issues to document)
  </read_first>
  <action>
Rewrite `README.md` in English (D-26) with these sections, in order, and a link to `README.ru.md` at the top:
1. What it is (one paragraph: self-hosted annotation + YOLO training service; Phase 1 delivers projects; roadmap link to `.planning/ROADMAP.md`).
2. Security notice (before any run instructions): no authentication in v1; by default only this computer can open it (`127.0.0.1:8080`); setting `BIND_ADDR=0.0.0.0` gives everyone on the network full access (D-14); `ALLOWED_HOSTS` must then include the LAN address or hostname; `.pt` model files are pickle-based and can execute code — only use files from trusted sources.
3. Quick start with Docker: prerequisites (Docker Desktop on Windows/macOS or Docker Engine + Compose v2 on Linux; CPU is enough); optional `cp .env.example .env`; `docker compose up --build` (first build downloads the PyTorch CPU wheel and takes several minutes); open http://127.0.0.1:8080; stop with `docker compose down`; update with `git pull` then `docker compose up --build`. Images are built locally (D-16).
4. Your data: everything lives in `./data` (or `DATA_DIR`), survives `down`/`up` and rebuilds, migrations run automatically on start (D-20); back up by stopping the stack and copying the folder; on Linux the files are owned by root because the containers run as root.
5. Configuration table for `.env`: `DATA_DIR` (./data), `BIND_ADDR` (127.0.0.1), `PORT` (8080), `ALLOWED_HOSTS` (localhost,127.0.0.1), `SQLITE_JOURNAL_MODE` (WAL).
6. Developer setup (FOUND-01, D-22): install uv (Windows PowerShell and macOS/Linux commands from the official Astral docs, https://docs.astral.sh/uv/getting-started/installation/); note that the system `python` may be a different version (for example Python 3.14) and that `uv` installs and uses Python 3.12 from `.python-version` automatically — always use `uv run`; `uv sync --locked` (installs the API, PyTorch 2.14.0 CPU, Ultralytics 8.4.159, pytest, ruff); Node.js 24 LTS + npm; `npm --prefix frontend ci`.
7. Running natively (D-19): terminal 1 `uv run uvicorn yolo_trainer_api.main:app --reload --port 8000` from the repo root (data in `./data`); terminal 2 `npm --prefix frontend run dev`, open http://localhost:5173 (Vite proxies `/api` to port 8000).
8. Tests and checks: `bash scripts/run_full_suite.sh` (everything below in one command; created in Task 2), `bash scripts/fresh_clone_check.sh` (rehearses these setup steps in a throwaway clone), `uv run pytest backend/tests`, `uv run ruff check backend`, `uv run ruff format --check backend`, `npm --prefix frontend run test -- --run`, `npm --prefix frontend run build`, `bash scripts/compose_smoke_test.sh` (uses its own `.smoke-data` and port 18080), `uv run python scripts/check_cli_run_git_clean.py` (needs the local `example_ready_dataset` images).
9. Legacy CLI scripts: `uv run python train_yolo.py`, `uv run python finetune_yolo.py` (configured by `config/config.ini`, outputs under `runs/`, both ignored by git); shared helpers live in `backend/src/yolo_trainer_common`; `gpu_test.py` fails on machines without an NVIDIA GPU (replaced by a diagnostics page in Phase 12); `train_yolo.py` reads `Config/Config.ini`, which only resolves on case-insensitive filesystems (Windows/default macOS).
10. Troubleshooting: SQLite "database disk image is malformed" on Docker Desktop (Windows/macOS) -> stop the stack, restore `./data` from backup if needed, set `SQLITE_JOURNAL_MODE=DELETE`, restart; native Linux Docker is not affected (RESEARCH Pitfall 1); port 8080 busy -> set `PORT`; "Invalid host header" (HTTP 400) -> add the host to `ALLOWED_HOSTS`.
11. Repository layout (backend/, frontend/, docker/, scripts/, root CLI scripts, config/, docs/, .planning/).
Do not document features that do not exist yet (images, classes, training in the UI) except as roadmap pointers.
  </action>
  <verify>
    <automated>for s in "README.ru.md" "docker compose up --build" "127.0.0.1:8080" "BIND_ADDR" "ALLOWED_HOSTS" "SQLITE_JOURNAL_MODE" "DATA_DIR" "uv sync --locked" "uv run pytest backend/tests" "run dev" "http://localhost:5173" "uvicorn yolo_trainer_api.main:app --reload" "compose_smoke_test.sh" "check_cli_run_git_clean.py" "authentication" "pickle"; do grep -qF -- "$s" README.md || { echo "MISSING: $s"; exit 1; }; done; echo README OK</automated>
    <fails_when>non-zero exit, or a line starting with `MISSING:`, or `README OK` absent</fails_when>
  </verify>
  <acceptance_criteria>
    - The verify loop prints `README OK` (all 16 required strings present)
    - `grep -n -i "no authentication\|without authentication" README.md` prints at least one line that appears before the first occurrence of `docker compose up --build`
    - `grep -c "http://localhost:5173" README.md` returns at least 1
  </acceptance_criteria>
  <done>README.md takes a user from clone to a running stack and a developer to a tested environment, with the security and persistence caveats stated up front.</done>
</task>

<task type="auto">
  <name>Task 2: Russian README translation, fresh-clone and full-suite scripts, and the final phase run</name>
  <precondition>Docker Desktop is running (`docker info` exits 0) and all earlier Phase 1 plans are committed (`git status --porcelain` is empty apart from this plan's files)</precondition>
  <files>README.ru.md, scripts/run_full_suite.sh, scripts/fresh_clone_check.sh</files>
  <read_first>
    - README.md (Task 1 — source text)
    - scripts/compose_smoke_test.sh (bash style to follow: set -euo pipefail, LF endings, final status line)
    - .planning/phases/01-runnable-skeleton-projects/01-VALIDATION.md (full suite command and manual-only list)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-26
  </read_first>
  <action>
1. Write `README.ru.md`: a faithful Russian translation of `README.md` with the same section order, identical commands, URLs, variable names and file paths (commands and code are not translated), and a link back to `README.md` at the top (D-26).
2. `scripts/fresh_clone_check.sh` (bash, `set -euo pipefail`): create a temp dir with `mktemp -d`, `git clone --quiet` the repo root (resolved from the script location) into it, and inside the clone run exactly the README developer steps — `uv sync --locked`, `uv run pytest backend/tests -x -q`, `npm --prefix frontend ci`, `npm --prefix frontend run test -- --run`; remove the temp dir via `trap`; print `FRESH CLONE OK` last. It tests the committed HEAD, so commit first.
3. `scripts/run_full_suite.sh` (bash, `set -euo pipefail`, runs from the repo root resolved from the script location): `uv run pytest backend/tests`, `uv run ruff check backend`, `uv run ruff format --check backend`, `npm --prefix frontend run test -- --run`, `npm --prefix frontend run build`, `bash scripts/compose_smoke_test.sh`, `uv run python scripts/check_cli_run_git_clean.py` (the 01-VALIDATION.md full suite plus lint/build and the git-clean check); print `ALL CHECKS OK` last.
4. Commit, run both scripts, and record every result in the SUMMARY. A fresh-clone failure means the README or the repo is missing a step — fix the cause (README.md/README.ru.md for documentation gaps; otherwise report it in the SUMMARY as a blocker) and re-run.
  </action>
  <verify>
    <automated>bash scripts/fresh_clone_check.sh</automated>
    <fails_when>non-zero exit, or `FRESH CLONE OK` absent from the output</fails_when>
    <automated>bash scripts/run_full_suite.sh</automated>
    <fails_when>non-zero exit, or the output lacks any of `SMOKE OK`, `GIT CLEAN OK`, `ALL CHECKS OK`</fails_when>
    <human-check>End-of-phase verification (all UI flows, both languages, dark theme):
      1. `docker compose up --build`, open http://127.0.0.1:8080 — dark theme only, no theme toggle.
      2. With the browser language set to Russian, the first load is Russian; switch to English in the header, reload — stays English.
      3. Create one `detect` and one `segment` project from the dashed "+ New project" card; try an empty name and a case-variant duplicate — clear errors, API text shown as-is.
      4. Open a project from its card: sidebar with "← Projects", Overview, Settings; Overview shows name, badge, description, created date, empty-state hint; reload on /projects/&lt;id&gt; still works.
      5. Settings: rename, edit description; task type is shown but cannot be changed; delete only after typing the exact name, then land on the grid with a confirmation.
      6. `docker compose down` then `docker compose up`: remaining projects are still there.
      7. From another device on the LAN, http://&lt;this-machine-ip&gt;:8080 is not reachable with the default settings.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `for s in "README.md" "docker compose up --build" "127.0.0.1:8080" "BIND_ADDR" "ALLOWED_HOSTS" "SQLITE_JOURNAL_MODE" "uv sync --locked"; do grep -qF -- "$s" README.ru.md || exit 1; done` exits 0
    - `grep -c "^## " README.md` equals `grep -c "^## " README.ru.md` (same number of top-level sections)
    - `bash scripts/fresh_clone_check.sh` exits 0 and prints `FRESH CLONE OK`
    - `bash scripts/run_full_suite.sh` exits 0 and its output contains `SMOKE OK`, `GIT CLEAN OK` and `ALL CHECKS OK`
  </acceptance_criteria>
  <done>Both READMEs are complete, a fresh clone passes the documented developer steps, and the entire Phase 1 suite is green; the UI flows are listed for the end-of-phase human check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> user configuration | Users follow the README to expose (or not) an unauthenticated service and to load model files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure / Elevation of Privilege | README LAN instructions | high | mitigate | Security notice placed before run instructions; explicit statement that `BIND_ADDR=0.0.0.0` grants full access to the network; default stays loopback (D-14) |
| T-10-02 | Elevation of Privilege | `.pt` pickle files | medium | mitigate | README states `.pt` files can execute code and must come from trusted sources (PROJECT constraint) |
| T-10-03 | Tampering (data loss) | SQLite on Docker Desktop | medium | mitigate | README documents backup, the WAL caveat and the `SQLITE_JOURNAL_MODE=DELETE` escape hatch (RESEARCH Pitfall 1 mitigations 4-5) |
| T-10-04 | Tampering | uv install instructions | low | mitigate | Point to the official Astral installation page instead of copying unverified mirrors |
</threat_model>

<verification>
- `bash scripts/fresh_clone_check.sh` prints `FRESH CLONE OK`
- `bash scripts/run_full_suite.sh` prints `ALL CHECKS OK` (includes `SMOKE OK` and `GIT CLEAN OK`)
- Human-check list executed at end of phase (human_verify_mode = end-of-phase)
</verification>

<success_criteria>
Phase 1 is demonstrably complete: documentation lets a newcomer reproduce the environment and run the service, and every automated check for FOUND-01..03, DEPL-01, DEPL-03, PROJ-01 and PROJ-02 passes.
</success_criteria>

## Artifacts this phase produces (Plan 10)

- `README.md` (English; sections: overview, security notice, Docker quick start, your data, configuration, developer setup, running natively, tests and checks, legacy CLI scripts, troubleshooting, repository layout).
- `README.ru.md` (Russian translation, same structure and commands).
- `scripts/run_full_suite.sh` (prints `ALL CHECKS OK`), `scripts/fresh_clone_check.sh` (prints `FRESH CLONE OK`).
- Verification evidence in `01-10-SUMMARY.md`: fresh-clone rehearsal and full-suite results.

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-10-SUMMARY.md` when done (include the full-suite and fresh-clone results; the human-check outcome is recorded by the end-of-phase verifier).
</output>
