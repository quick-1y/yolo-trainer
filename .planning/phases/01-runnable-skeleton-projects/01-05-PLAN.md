---
phase: 01-runnable-skeleton-projects
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-04]
files_modified:
  - .gitignore
  - scripts/check_cli_run_git_clean.py
  - trained_models/best.pt
  - yolo26n.pt
  - yolov8n.pt
  - runs/
  - docs/roadmap.md
autonomous: true
requirements: [FOUND-03]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`uv run python scripts/check_cli_run_git_clean.py` runs a real 1-epoch CPU training through train_yolo.train() on example_ready_dataset and then prints `GIT CLEAN OK` because `git status --porcelain` is identical before and after the run"
    - "`git ls-files` lists no *.pt file and nothing under runs/ or trained_models/"
    - "trained_models/best.pt, yolo26n.pt and yolov8n.pt still exist on disk with the same SHA-256 as before untracking"
    - "example_ready_dataset/train/, valid/ and test/ are ignored, while example_ready_dataset/data.yaml, README.dataset.txt and README.roboflow.txt remain tracked"
    - "data/, .smoke-data/, .env and .planning/research/.cache/ are ignored"
    - "docs/roadmap.md §1.7 and §7.2 describe example_ready_dataset as predominantly detect-format with a few stray polygon rows (2/2/4 in train/valid/test), and §5.7 notes that D-13 replaces named volumes with the ./data bind mount"
  prohibitions:
    - statement: "MUST NOT delete or alter the on-disk trained_models/best.pt, yolo26n.pt, yolov8n.pt or runs/ contents while untracking them, and MUST NOT rewrite git history (no filter-repo, filter-branch, rebase or force-push)"
      status: resolved
      verification: test
  artifacts:
    - path: ".gitignore"
      provides: "ignore rules for weights, runs, datasets, app data, caches"
      contains: "example_ready_dataset/train/"
    - path: "scripts/check_cli_run_git_clean.py"
      provides: "automated FOUND-03 proof (real CLI training run leaves git status unchanged)"
      contains: "GIT CLEAN OK"
    - path: "docs/roadmap.md"
      provides: "corrected example-dataset characterization (Phase 0 follow-up #4)"
  key_links:
    - from: "scripts/check_cli_run_git_clean.py"
      to: "train_yolo.py"
      via: "imports train_yolo and calls train(section) with a tiny in-memory [Train] config"
      pattern: "train_yolo.train"
---

## Phase Goal

ROADMAP goal (verbatim): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." (Not in user-story form; see Plan 01 note.)

<objective>
Expansion slice for repository hygiene (FOUND-03, success criterion 3): after a CLI training run `git status` is clean, previously committed binaries are untracked without touching the owner's real `trained_models/best.pt` (D-25), the example dataset's images are ignored (D-23), and the roadmap's wrong dataset characterization is corrected (Phase 0 follow-up #4).

Purpose: keep weights, runs, datasets and app data out of git from here on, proven by an automated real training run instead of a manual check.
Output: updated `.gitignore`, untracked binaries (index only), `scripts/check_cli_run_git_clean.py`, amended `docs/roadmap.md`.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-projects/01-04-SUMMARY.md

<interfaces>
`train_yolo.train(config)` takes a configparser SectionProxy and reads: `verbose` (bool, fallback False), `device`, `imgsz` (int), `epochs` (int), `name_model`, `dataset_path` (it joins `data.yaml`), `workers` (int), `optimizer`, `pretrained` (bool), `amp` (bool), `augment` (bool), `project`, `name`. Batch comes from the local `auto_batch(imgsz)`. It trains, then runs `model.val()` and prints a Rich table. After Plan 04 it imports `parse_device`/`quality_assessment` from `yolo_trainer_common`.

Tracked binaries today (`git ls-files`): `trained_models/best.pt`, `yolo26n.pt`, `yolov8n.pt`, 43 files under `runs/detect/...`. Untracked: `example_ready_dataset/{train,valid,test}/` (images, labels, Ultralytics `labels.cache`), `.planning/research/.cache/`.
</interfaces>

<assumption_delta_decision>
Detector signal: `pluralization` on the ROADMAP note "Also decide whether the `example_ready_dataset/` images are a committed fixture or ignored".
- Primary noun: `project` (identity = integer `id`; name identity = `normalized_name`) — unchanged by this phase.
- Decision: `no-change`. The signal is prose about the example dataset's git disposition, already locked by D-23 (images ignored; a dedicated small fixture arrives with the phase that needs it). No identity model is generalized from singular to plural in Phase 1.
</assumption_delta_decision>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ignore rules, index-only untracking of binaries, and an automated "CLI run leaves git clean" check</name>
  <reversibility rating="costly">D-25 untracking changes what every future clone receives; re-adding weights later would bloat history again. No history rewrite happens here.</reversibility>
  <files>.gitignore, scripts/check_cli_run_git_clean.py, trained_models/best.pt (index only), yolo26n.pt (index only), yolov8n.pt (index only), runs/ (index only)</files>
  <read_first>
    - .gitignore (current rules incl. Plan 01 additions)
    - train_yolo.py (the `train(config)` function the check drives) and config/config.ini ([Train] keys)
    - .planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md "Runtime State Inventory" (exact untracking procedure) and "Anti-Patterns" (untracking must keep the working tree)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-23, D-25
  </read_first>
  <action>
1. Record SHA-256 of `trained_models/best.pt`, `yolo26n.pt`, `yolov8n.pt` (e.g. `sha256sum`) into the SUMMARY before any git operation.
2. `.gitignore`: replace the comment claiming `example_ready_dataset/` is intentionally not ignored with the D-23 rule and add `example_ready_dataset/train/`, `example_ready_dataset/valid/`, `example_ready_dataset/test/` (images, labels and Ultralytics `labels.cache` live there; `data.yaml` and the two README files stay tracked). Add `.planning/research/.cache/`. Keep every existing rule (`*.pt`, `runs/`, `trained_models/`, Plan 01's `data/`, `.smoke-data/`, `.env`, caches).
3. Untrack with index-only removal (D-25): `git rm --cached trained_models/best.pt yolo26n.pt yolov8n.pt` and `git rm -r --cached runs/`. Never use the non-cached form and never `--force`; no history rewrite, no push. Re-hash the three weight files and confirm the hashes match step 1.
4. Create `scripts/check_cli_run_git_clean.py` (English comments, type hints, stdlib + the project venv only): resolve the repo root from `__file__`, `os.chdir` there and put it on `sys.path`; if `example_ready_dataset/train/images` is missing, print a clear message and exit with status 3 (never a silent pass); capture `git status --porcelain --untracked-files=all` via `subprocess.run(..., check=True)`; build an in-memory `configparser.ConfigParser` whose `[Train]` section has `dataset_path = example_ready_dataset`, `name_model = yolov8n.pt`, `epochs = 1`, `imgsz = 64`, `device = cpu`, `workers = 0`, `optimizer = SGD`, `pretrained = True`, `amp = False`, `augment = False`, `project = runs/hygiene-check`, `name = run`, `verbose = False`; `import train_yolo` and call `train_yolo.train(conf["Train"])` (the real legacy code path, FOUND-02 + FOUND-03); capture status again; if it differs, print both listings and exit 1; otherwise remove `runs/hygiene-check` and print `GIT CLEAN OK`.
5. Run the check. If it reports new untracked paths, add the narrowest ignore rule that covers them (e.g. a download location Ultralytics used) and re-run until it prints `GIT CLEAN OK`; record any added rule in the SUMMARY.
  </action>
  <verify>
    <automated>uv run python scripts/check_cli_run_git_clean.py</automated>
    <fails_when>non-zero exit (1 = git status changed, 3 = dataset images missing), or `GIT CLEAN OK` absent from the output</fails_when>
    <automated>test -z "$(git ls-files -- '*.pt' 'runs/' 'trained_models/')" && echo UNTRACKED OK</automated>
    <fails_when>non-zero exit, or `UNTRACKED OK` absent (some weight/run path is still tracked)</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run python scripts/check_cli_run_git_clean.py` exits 0 and prints `GIT CLEAN OK`
    - `git ls-files` contains no path ending in `.pt` and no path under `runs/` or `trained_models/`
    - `test -f trained_models/best.pt && test -f yolo26n.pt && test -f yolov8n.pt` exits 0 and the SHA-256 values equal those recorded in step 1 (listed in the SUMMARY)
    - `git ls-files example_ready_dataset` lists exactly `data.yaml`, `README.dataset.txt`, `README.roboflow.txt` (three lines, no images or labels)
    - `for p in data/x .smoke-data/x .env example_ready_dataset/train/images/x.jpg example_ready_dataset/test/labels.cache .planning/research/.cache/x; do git check-ignore -q "$p" || exit 1; done` exits 0 (every path is ignored)
    - `git log --oneline -1 -- trained_models/best.pt` still shows history (no rewrite) and `git reflog` shows no reset/rebase entries created by this plan
  </acceptance_criteria>
  <done>Weights and runs are untracked but intact on disk, generated artifacts are ignored, and a real CLI training run is proven to leave `git status` unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the example-dataset characterization in docs/roadmap.md (Phase 0 follow-up #4) and note the D-13 persistence override</name>
  <files>docs/roadmap.md</files>
  <read_first>
    - docs/roadmap.md §1.7 (lines ~82-91), §5.7 (lines ~278-282), §7.2 (lines ~330-340)
    - docs/phase-0-decisions.md §3 (the corrected facts: 278/280 train, 71/73 valid, 36/40 test objects are box rows)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-13, D-23
  </read_first>
  <action>
Edit only the three sections:
- §1.7: rewrite the "Label format" bullet so its main text states the dataset is predominantly detection-format (plain 5-value box rows: 278/280 train, 71/73 valid, 36/40 test objects) with a few stray polygon rows (2/2/4 per split), and is a `detect`-task example, not a `segment` one; keep a one-line pointer to `docs/phase-0-decisions.md` §3 for the history of the correction (the old single-file finding may be summarized in one sentence, not repeated as the headline). Update the "Size on disk" bullet: the images/labels are no longer tracked — only `data.yaml` and the two README files are (D-23), and a small dedicated test fixture is added by the phase that first needs one.
- §7.2: rewrite the "Product implication" bullet so it no longer says `license-plate` is polygon-annotated; keep OBB for plates only as a possible future direction.
- §5.7: add one short note that Phase 1 decision D-13 stores app-owned data in a host bind-mounted `./data` folder (overridable via `DATA_DIR`) instead of named volumes, with the Docker Desktop SQLite/WAL caveat handled by Phase 1's mitigations.
Keep the document's existing English style; do not touch other sections.
  </action>
  <verify>
    <automated>grep -c "predominantly" docs/roadmap.md</automated>
    <fails_when>output is 0, or grep exits non-zero</fails_when>
    <automated>grep -n "D-13" docs/roadmap.md</automated>
    <fails_when>no line printed (exit status 1)</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### 1.7/,/^### 1.8/p' docs/roadmap.md | grep -c "predominantly"` returns at least 1 and the same section contains `2/2/4`
    - `sed -n '/^### 7.2/,/^### 7.3/p' docs/roadmap.md | grep -ci "annotated with full polygons today"` returns 0 (the phrase exists before this task, so the check discriminates)
    - `sed -n '/^### 5.7/,/^### 5.8/p' docs/roadmap.md | grep -c "D-13"` returns at least 1
    - `git diff --stat "$PLAN_BASE" -- docs/roadmap.md` (PLAN_BASE = commit before Task 2) shows only docs/roadmap.md changed by this task
  </acceptance_criteria>
  <done>The roadmap no longer misleads later planners about the example dataset or about where app data lives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> git index/history | Irreversible-feeling operations on the owner's only copy of `trained_models/best.pt` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / Denial of Service (data loss) | untracking step | high | mitigate | Index-only `git rm --cached`, no `--force`; SHA-256 recorded before and verified after; acceptance checks file existence and hashes |
| T-05-02 | Repudiation / Tampering | git history | high | mitigate | No history rewrite, reset, rebase or push in this plan (D-25); acceptance checks history still contains best.pt and reflog has no rewrite entries |
| T-05-03 | Information Disclosure | future commits of datasets/app data | medium | mitigate | Ignore rules for `data/`, `.env`, dataset split dirs, runs, weights; automated check proves a training run leaves status unchanged |
| T-05-04 | Elevation of Privilege | `yolov8n.pt` pickle load in the check script | low | accept | Loads the repo's own Ultralytics base weights (or Ultralytics' official download) exactly as the legacy script already does |
</threat_model>

<verification>
- `uv run python scripts/check_cli_run_git_clean.py` -> `GIT CLEAN OK`
- `git ls-files` has no weights/runs; weights still on disk with unchanged hashes
- docs/roadmap.md §1.7/§7.2/§5.7 amended
</verification>

<success_criteria>
Phase 1 success criterion 3: after a CLI training run `git status` is clean, earlier-committed binaries are untracked, and the real `trained_models/best.pt` stays on disk.
</success_criteria>

## Artifacts this phase produces (Plan 05)

- `scripts/check_cli_run_git_clean.py` (exit codes: 0 clean, 1 status changed, 3 dataset images missing; prints `GIT CLEAN OK`).
- `.gitignore` rules `example_ready_dataset/train/`, `example_ready_dataset/valid/`, `example_ready_dataset/test/`, `.planning/research/.cache/`.
- Index change: `trained_models/best.pt`, `yolo26n.pt`, `yolov8n.pt`, `runs/**` untracked (working tree kept).
- `docs/roadmap.md` amended §1.7, §5.7, §7.2.

## Flagged assumptions (edge probe, unresolved)

- FOUND-03 (`unclassified` probe): unresolved by the probe. Planner coverage: the automated check drives the real legacy training path; artifacts Ultralytics may write outside the repo (user settings/weights cache) are out of git's scope by definition.

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-05-SUMMARY.md` when done (include the before/after SHA-256 values).
</output>
