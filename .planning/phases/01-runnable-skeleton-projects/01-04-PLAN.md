---
phase: 01-runnable-skeleton-projects
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - backend/src/yolo_trainer_common/__init__.py
  - backend/src/yolo_trainer_common/device.py
  - backend/src/yolo_trainer_common/quality.py
  - backend/tests/test_device.py
  - backend/tests/test_quality.py
  - backend/pyproject.toml
  - pyproject.toml
  - uv.lock
  - train_yolo.py
  - finetune_yolo.py
autonomous: true
requirements: [FOUND-01, FOUND-02]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "parse_device and quality_assessment exist once, in backend/src/yolo_trainer_common, and train_yolo.py and finetune_yolo.py import them instead of defining local copies"
    - "Apart from their import blocks, train_yolo.py and finetune_yolo.py are unchanged; auto_batch stays a local function in train_yolo.py and is not added to the shared module"
    - "`uv sync --locked` at the repo root yields a Python 3.12 venv in which torch 2.14.0 (CPU build), torchvision 0.29.0 and ultralytics 8.4.159 import successfully"
    - "Both pyproject files declare requires-python >=3.12,<3.13; torch==2.14.0, torchvision==0.29.0 and ultralytics==8.4.159 are exact pins (no ranges) in the backend `worker` extra"
    - "torch and torchvision resolve only from the explicit `pytorch-cpu` uv index (uv.lock records download.pytorch.org/whl/cpu for them); every other package resolves from PyPI"
    - "detect_compute_device() reports cuda_available False and device 'cpu' without calling torch.cuda.get_device_name when CUDA is unavailable, and reports the GPU name when it is available"
    - "parse_device keeps its original behavior: 'cpu' and ' CPU ' -> 'cpu'; '0' -> 0; ' 1 ' -> 1; '0,1' -> [0, 1]; 'abc' raises ValueError"
    - "quality_assessment boundaries: 0.90 -> excellent, 0.8999 -> good, 0.75 -> good, 0.7499 -> average, 0.60 -> average, 0.5999 -> needs training (same Russian strings as the original)"
  artifacts:
    - path: "backend/src/yolo_trainer_common/device.py"
      provides: "parse_device, detect_compute_device (lazy torch import)"
      exports: ["parse_device", "detect_compute_device", "ComputeDevice"]
    - path: "backend/src/yolo_trainer_common/quality.py"
      provides: "quality_assessment"
      exports: ["quality_assessment"]
    - path: "backend/tests/test_device.py"
      provides: "unit tests for parse_device / detect_compute_device"
    - path: "backend/tests/test_quality.py"
      provides: "unit tests for quality_assessment thresholds"
    - path: "uv.lock"
      provides: "locked torch/torchvision from the pytorch-cpu index + ultralytics pin"
      contains: "download.pytorch.org/whl/cpu"
  key_links:
    - from: "train_yolo.py"
      to: "backend/src/yolo_trainer_common/device.py"
      via: "from yolo_trainer_common.device import parse_device"
      pattern: "from yolo_trainer_common.device import parse_device"
    - from: "finetune_yolo.py"
      to: "backend/src/yolo_trainer_common/quality.py"
      via: "from yolo_trainer_common.quality import quality_assessment"
      pattern: "from yolo_trainer_common.quality import quality_assessment"
    - from: "pyproject.toml"
      to: "backend/pyproject.toml"
      via: "root dependency yolo-trainer-backend[worker] (workspace source) installs the shared module and ML stack into the root .venv"
      pattern: "yolo-trainer-backend\\[worker\\]"
---

## Phase Goal

ROADMAP goal (verbatim): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." (Not in user-story form; see Plan 01 note.)

<objective>
Expansion slice for the developer path (FOUND-01, FOUND-02): the duplicated helpers from the legacy CLI scripts become one shared, unit-tested module, and a fresh `uv sync` produces the pinned Python 3.12 + PyTorch 2.14.0 (CPU) + Ultralytics 8.4.159 environment in which `python train_yolo.py` / `python finetune_yolo.py` still run exactly as before.

Purpose: success criteria 1 and 2 of Phase 1; the same shared module is what the worker image (Plan 06) and later the training worker (Phase 4) import.
Output: `yolo_trainer_common` package + tests, `worker` extra with pinned ML stack from the CPU wheel index, refreshed `uv.lock`, legacy scripts importing the shared module.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md
@.planning/phases/01-runnable-skeleton-projects/01-PATTERNS.md
@.planning/phases/01-runnable-skeleton-projects/01-01-SUMMARY.md

<interfaces>
Current duplicated helpers (identical in both scripts; extract behavior verbatim, PATTERNS.md "Pattern Assignments"):
- `parse_device(device_str)`: strip; `"cpu"` (case-insensitive) -> `"cpu"`; contains `","` -> list of ints; else `int(device_str)`.
- `quality_assessment(map50)`: `>= 0.90` -> "🔥 Отличная модель"; `>= 0.75` -> "👍 Хороший результат"; `>= 0.60` -> "⚠️ Среднее качество (можно улучшить)"; else "❌ Требуется дообучение".
- `auto_batch(imgsz, gpu_vram=12)` exists only in `train_yolo.py` — it stays there (RESEARCH "State of the Art": known-bad heuristic, not centralized).

Workspace layout from Plan 01: root `pyproject.toml` (`yolo-trainer`, non-package, `dependencies = ["yolo-trainer-backend"]`, dev group), member `backend/pyproject.toml` (`yolo-trainer-backend`, hatchling, `packages = ["src/yolo_trainer_api"]`). Docker `api` stage runs `uv sync --frozen --no-dev --package yolo-trainer-backend` (no extras) — it must stay torch-free after this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared yolo_trainer_common module (parse_device, quality_assessment, detect_compute_device) with unit tests</name>
  <files>backend/src/yolo_trainer_common/__init__.py, backend/src/yolo_trainer_common/device.py, backend/src/yolo_trainer_common/quality.py, backend/tests/test_device.py, backend/tests/test_quality.py, backend/pyproject.toml</files>
  <read_first>
    - train_yolo.py (lines 16-41: the helper section) and finetune_yolo.py (lines 17-37) — the source of truth being extracted
    - gpu_test.py (shows the unguarded get_device_name call the new helper must avoid)
    - .planning/phases/01-runnable-skeleton-projects/01-PATTERNS.md sections for device.py and quality.py
    - backend/pyproject.toml (hatch packages list to extend)
  </read_first>
  <behavior>
    - parse_device("cpu") == "cpu"; parse_device(" CPU ") == "cpu"; parse_device("0") == 0; parse_device(" 1 ") == 1; parse_device("0,1") == [0, 1]; parse_device("abc") raises ValueError
    - quality_assessment(0.90) == "🔥 Отличная модель"; 0.8999 -> "👍 Хороший результат"; 0.75 -> "👍 Хороший результат"; 0.7499 -> "⚠️ Среднее качество (можно улучшить)"; 0.60 -> same; 0.5999 -> "❌ Требуется дообучение"
    - detect_compute_device() with a fake torch module in sys.modules whose cuda.is_available() is False -> ComputeDevice(cuda_available=False, device="cpu", device_name=None); fake get_device_name raises if called, proving it is never called
    - detect_compute_device() with fake CUDA available and get_device_name(0) == "Fake GPU" -> cuda_available True, device "cuda:0", device_name "Fake GPU"
    - importing yolo_trainer_common.device does not import torch (torch absent from sys.modules after import in a subprocess)
  </behavior>
  <action>
Tests first (RED), then implement (GREEN):
- `quality.py`: `quality_assessment(map50: float) -> str` with the exact thresholds and exact Russian user-facing strings from the legacy scripts (they are output text, not comments, so they are NOT translated — PATTERNS.md).
- `device.py`: `parse_device(device_str: str) -> str | int | list[int]` with identical behavior (including `ValueError` on garbage via `int()`); frozen dataclass `ComputeDevice(cuda_available: bool, device: str, device_name: str | None)`; `detect_compute_device() -> ComputeDevice` that imports `torch` lazily inside the function and only calls `torch.cuda.get_device_name(0)` after `torch.cuda.is_available()` returns True (fixes the `gpu_test.py` crash pattern for new code; `gpu_test.py` itself stays unchanged until Phase 12 per D-21). The module must be importable in the torch-free api image.
- `__init__.py`: re-export `parse_device`, `quality_assessment`, `detect_compute_device`, `ComputeDevice`.
- `backend/pyproject.toml`: add `"src/yolo_trainer_common"` to `[tool.hatch.build.targets.wheel] packages`.
New code uses English comments and type hints and passes ruff (D-24); tests inject a fake torch through `monkeypatch.setitem(sys.modules, "torch", fake)` so they run without CUDA hardware.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/test_device.py backend/tests/test_quality.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran", or fewer than 10 passed</fails_when>
    <automated>uv run ruff check backend && uv run ruff format --check backend</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/test_device.py backend/tests/test_quality.py -q` exits 0 with at least 10 passed
    - `grep -c "src/yolo_trainer_common" backend/pyproject.toml` returns 1
    - `grep -Ec "^import torch|^from torch" backend/src/yolo_trainer_common/device.py` returns 0 (torch is imported lazily inside the function only)
    - `grep -c "auto_batch" backend/src/yolo_trainer_common/device.py` returns 0
  </acceptance_criteria>
  <done>The shared helpers live in one tested module, importable without torch, with GPU-name lookup guarded.</done>
</task>

<task type="auto">
  <name>Task 2: Pinned ML stack via uv (CPU wheel index) and legacy scripts switched to the shared module</name>
  <files>pyproject.toml, backend/pyproject.toml, uv.lock, train_yolo.py, finetune_yolo.py</files>
  <read_first>
    - pyproject.toml, backend/pyproject.toml, uv.lock (Plan 01 state)
    - train_yolo.py, finetune_yolo.py (full files — only the import block and the helper section change)
    - .planning/phases/01-runnable-skeleton-projects/01-RESEARCH.md "Architecture Patterns" Pattern 4, "Common Pitfalls" 2 and 4, "Open Questions" 1
    - docs/phase-0-decisions.md §1 (the pins being re-verified)
  </read_first>
  <action>
1. Re-verify pins at implementation time (ROADMAP note, D-22): run `python -m pip index versions ultralytics` and `python -m pip index versions torch`, and list `https://download.pytorch.org/whl/cpu/torchvision/` for the 0.29.0 cp312 wheels. Keep `torch==2.14.0`, `torchvision==0.29.0` (the release paired with torch 2.14.0; cp312 CPU wheels exist for linux x86_64/aarch64, win_amd64 and macOS arm64 — checked 2026-09-24) and `ultralytics==8.4.159` (the Phase-0 spike-tested pin) unless one of them is yanked or missing; record the checked versions in the SUMMARY. If a pin is unavailable, stop and report instead of silently choosing another version.
2. `backend/pyproject.toml`: add `[project.optional-dependencies] worker = ["torch==2.14.0", "torchvision==0.29.0", "ultralytics==8.4.159"]`.
3. Root `pyproject.toml` (workspace root; D-22): change dependencies to `["yolo-trainer-backend[worker]", "rich>=13"]` (rich is used only by the legacy CLI scripts); add `[[tool.uv.index]] name = "pytorch-cpu"`, `url = "https://download.pytorch.org/whl/cpu"`, `explicit = true`; add `torch = { index = "pytorch-cpu" }` and `torchvision = { index = "pytorch-cpu" }` to `[tool.uv.sources]` (RESEARCH Pattern 4 — everything else still comes from PyPI). If uv reports that sources/indexes must live in the member that declares the dependency, move both into `backend/pyproject.toml` instead; the acceptance checks below are what matter.
4. `uv lock` then `uv sync --locked`. Confirm the Docker api stage stays torch-free: `uv export --frozen --no-dev --package yolo-trainer-backend --no-hashes` must not list torch (RESEARCH Pitfall 4).
5. Legacy scripts (D-21, D-24 — only imports change; Russian comments, Rich output, configparser use, `if __name__ == "__main__":` blocks and every other line stay byte-identical): in `train_yolo.py` delete the local `parse_device` and `quality_assessment` definitions and add `from yolo_trainer_common.device import parse_device` and `from yolo_trainer_common.quality import quality_assessment` to the import block; keep the local `auto_batch` function exactly where it is. In `finetune_yolo.py` delete both local definitions and add the same two imports. Do not touch `gpu_test.py` or `config/config.ini`. Do NOT change the pre-existing `Config/Config.ini` path in `train_yolo.py` (D-24 allows only import changes; the case mismatch is reported in the SUMMARY as a known Linux/macOS issue for the owner to decide).
  </action>
  <verify>
    <automated>uv sync --locked && uv run python -c "import torch, torchvision, ultralytics; assert torch.__version__.startswith('2.14.0'), torch.__version__; assert torchvision.__version__.startswith('0.29.0'), torchvision.__version__; assert ultralytics.__version__ == '8.4.159', ultralytics.__version__; print('PINS OK', torch.__version__, ultralytics.__version__)"</automated>
    <fails_when>non-zero exit, an AssertionError naming the wrong version, or `PINS OK` absent from the output</fails_when>
    <automated>uv run python -c "import train_yolo, finetune_yolo, yolo_trainer_common.device as d, yolo_trainer_common.quality as q; assert train_yolo.parse_device is d.parse_device and finetune_yolo.parse_device is d.parse_device; assert train_yolo.quality_assessment is q.quality_assessment and finetune_yolo.quality_assessment is q.quality_assessment; assert callable(train_yolo.auto_batch); print('IMPORTS OK')"</automated>
    <fails_when>non-zero exit, an AssertionError, or `IMPORTS OK` absent from the output</fails_when>
    <automated>uv run pytest backend/tests -x</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Both verify commands print `PINS OK` and `IMPORTS OK`
    - `grep -c "download.pytorch.org/whl/cpu" uv.lock` returns at least 1
    - `uv export --frozen --no-dev --package yolo-trainer-backend --no-hashes` output contains no line starting with `torch` (api image stays light, D-17)
    - `grep -c "def parse_device" train_yolo.py finetune_yolo.py` reports 0 for both files and `grep -c "def quality_assessment" train_yolo.py finetune_yolo.py` reports 0 for both files
    - `grep -c "def auto_batch" train_yolo.py` returns 1
    - `git diff "$PLAN_BASE" -- train_yolo.py finetune_yolo.py` (PLAN_BASE = commit before Task 2) shows only removed helper definitions and added `from yolo_trainer_common...` import lines
    - `grep -c "==2.14.0" backend/pyproject.toml` returns at least 1 and `grep -c "==8.4.159" backend/pyproject.toml` returns 1
  </acceptance_criteria>
  <done>A fresh `uv sync` gives the pinned CPU ML stack, both legacy scripts import the shared helpers and still import cleanly, and the api dependency set stays torch-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> PyPI / download.pytorch.org | Large binary wheels installed into the dev venv and (Plan 06) the worker image |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | uv index config / uv.lock | high | mitigate | `pytorch-cpu` index is `explicit = true` and scoped to torch/torchvision only (no dependency-confusion fallback for other names); exact pins; `uv.lock` records hashes and is committed; Docker uses `--frozen` |
| T-04-01 | Denial of Service | detect_compute_device | low | mitigate | Guard `get_device_name` behind `cuda.is_available()`; lazy torch import keeps the api image import-safe |
| T-04-02 | Tampering (behavior drift) | legacy scripts | low | mitigate | Identity-import check + diff limited to import lines; boundary unit tests pin original behavior |
| T-04-03 | Elevation of Privilege | pickle-based `.pt` loading by ultralytics | low | accept | No new `.pt` loading surface in this plan; the CLI scripts load the owner's own weights as before; upload-time risk is handled in Phase 7 |
</threat_model>

<verification>
- `uv sync --locked` + pin assertion prints `PINS OK`
- identity-import check prints `IMPORTS OK`
- `uv run pytest backend/tests -x` green
</verification>

<success_criteria>
Phase 1 success criteria 1-2: a fresh clone reaches the pinned Python 3.12 / PyTorch 2.14.0 / Ultralytics 8.4.159 stack with `uv sync`, pytest passes, and the legacy scripts share one tested helper module while behaving as before.
</success_criteria>

## Artifacts this phase produces (Plan 04)

- Package `yolo_trainer_common` with `parse_device(device_str) -> str | int | list[int]`, `quality_assessment(map50) -> str`, `ComputeDevice` (frozen dataclass: `cuda_available`, `device`, `device_name`), `detect_compute_device() -> ComputeDevice`.
- Tests `backend/tests/test_device.py`, `backend/tests/test_quality.py`.
- `[project.optional-dependencies] worker` in `backend/pyproject.toml`; uv index `pytorch-cpu` (explicit); root dependency `yolo-trainer-backend[worker]` + `rich`; refreshed `uv.lock`.
- Modified import blocks in `train_yolo.py`, `finetune_yolo.py`.

## Edge coverage (spec-less probe fallback, resolved here)

- FOUND-01 / adjacency: explicit — exact pins and `>=3.12,<3.13`: Python 3.13 and neighbouring torch/ultralytics patches are excluded rather than silently accepted.
- FOUND-01 / ordering: explicit — index precedence is deterministic: torch/torchvision only from `pytorch-cpu`, all else from PyPI, frozen by `uv.lock`.

## Flagged assumptions (edge probe, unresolved)

- FOUND-02 (`unclassified` probe): unresolved by the probe; planner coverage is the boundary tests in Task 1 (threshold equality at 0.90/0.75/0.60, whitespace/case/list inputs to `parse_device`).
- Known pre-existing issue, intentionally not fixed (D-24 "only imports change"): `train_yolo.py` reads `Config/Config.ini` (capital C) while the file is `config/config.ini`; this works on Windows but not on case-sensitive filesystems. Owner decision needed.

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-04-SUMMARY.md` when done (record the re-verified pin versions and whether uv sources/indexes live in the root or the member pyproject).
</output>
