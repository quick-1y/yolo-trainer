---
phase: 01-runnable-skeleton-projects
plan: 08
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - backend/src/yolo_trainer_api/routers/projects.py
  - backend/tests/test_projects_api.py
  - frontend/src/api/projects.ts
  - frontend/src/app/routes.tsx
  - frontend/src/features/projects/ProjectCard.tsx
  - frontend/src/features/project/ProjectLayout.tsx
  - frontend/src/features/project/ProjectOverviewPage.tsx
  - frontend/src/features/project/ProjectOverviewPage.test.tsx
  - frontend/src/features/project/ProjectNotFound.tsx
  - frontend/src/features/NotFoundPage.tsx
  - frontend/src/features/NotFound.test.tsx
  - frontend/src/i18n/locales/en/project.json
  - frontend/src/i18n/locales/ru/project.json
autonomous: true
requirements: [PROJ-02]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicking a project card opens /projects/:projectId, which shows a sidebar shell with a '← Projects' back link and an 'Overview' entry, and no links to sections that do not exist yet (D-11)"
    - "The Overview shows the project name, its task-type badge, the description (or a translated 'no description' hint when null), the created date formatted for the active language, and an empty-state hint"
    - "GET /api/projects/{id} returns the project; an unknown id returns 404 with detail 'Project not found.'; a non-integer id returns 422"
    - "Opening a project id that does not exist or was deleted in another tab shows a translated not-found state with a link back to the project list instead of a blank page or crash"
    - "Any unknown SPA route renders a translated not-found page with a link to /projects"
  artifacts:
    - path: "frontend/src/features/project/ProjectLayout.tsx"
      provides: "project sidebar shell (back link + Overview nav) with Outlet"
    - path: "frontend/src/features/project/ProjectOverviewPage.tsx"
      provides: "Overview section"
    - path: "frontend/src/api/projects.ts"
      provides: "useProject(id) hook"
      contains: "useProject"
    - path: "backend/src/yolo_trainer_api/routers/projects.py"
      provides: "GET /api/projects/{project_id}"
      contains: "project_id"
  key_links:
    - from: "frontend/src/features/projects/ProjectCard.tsx"
      to: "/projects/:projectId"
      via: "card rendered as a router Link to `/projects/${project.id}`"
      pattern: "/projects/\\$\\{"
    - from: "frontend/src/features/project/ProjectOverviewPage.tsx"
      to: "/api/projects/{project_id}"
      via: "useProject(projectId) -> apiRequest GET"
      pattern: "useProject"
    - from: "frontend/src/app/routes.tsx"
      to: "frontend/src/features/project/ProjectLayout.tsx"
      via: "nested route /projects/:projectId with index -> ProjectOverviewPage"
      pattern: "ProjectLayout"
---

## Phase Goal

ROADMAP goal (verbatim): "A user can start the service with one `docker compose up` on a CPU-only machine, open it in the browser, and create and manage `detect`/`segment` projects that persist, on top of a clean, reproducible, tested codebase." (Not in user-story form; see Plan 01 note.)

<objective>
Vertical slice "open a project" (PROJ-02 open, D-11): from the card grid the user opens a project and lands in the sidebar shell on its Overview; bad or stale links degrade to a clear not-found state.

Purpose: establishes the per-project shell that Plan 09 (Settings) and Phases 2-5 (Images, Classes, Training, Models) extend with new sidebar entries.
Output: `GET /api/projects/{project_id}`, `useProject`, project routes, `ProjectLayout`, `ProjectOverviewPage`, not-found handling, tests.
</objective>

<execution_context>
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/admin/PycharmProjects/yolo-trainer/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md
@.planning/phases/01-runnable-skeleton-projects/01-03-SUMMARY.md

<interfaces>
From Plan 01/03: router `APIRouter(prefix="/api/projects")` with list/create; `ProjectRead`; `get_session` dependency; error bodies `{"detail": "<plain English>"}`. Frontend: `apiRequest`, `ApiError(status)`, `Project`, `projectKeys = { all: ['projects'] }`, `useProjects`, `useCreateProject`; `routes.tsx` with `/` -> `/projects` redirect and `/projects` -> `ProjectsPage`; `ProjectCard` (name, badge, relative time); Vitest helper `renderWithProviders(ui, { route, language })`; i18n namespaces auto-loaded from `src/i18n/locales/{en,ru}/*.json`.
Plan 07 runs in the same wave and edits `AppLayout.tsx`, `i18n/index.ts`, `i18n/language.ts`, `locales/*/common.json` — do not touch those files here; put all new strings in the new `project` namespace (Plan 07's parity test will check it).
New API: `GET /api/projects/{project_id}` -> 200 `Project` | 404 `{"detail": "Project not found."}` | 422 for non-integer ids.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/projects/{project_id} with 404 for unknown ids</name>
  <files>backend/src/yolo_trainer_api/routers/projects.py, backend/tests/test_projects_api.py</files>
  <read_first>
    - backend/src/yolo_trainer_api/routers/projects.py (current list/create)
    - backend/tests/test_projects_api.py and backend/tests/conftest.py (fixtures)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-05, D-11
  </read_first>
  <behavior>
    - create "Cars" -> GET /api/projects/{id} returns 200 with the same body as in the list
    - GET /api/projects/999999 -> 404, detail == "Project not found."
    - GET /api/projects/abc -> 422 with a string detail
  </behavior>
  <action>
Tests first. Add `GET "/{project_id}"` (`project_id: int` path param) returning `ProjectRead`; when `session.get(Project, project_id)` is None raise `HTTPException(404, "Project not found.")` (plain English, D-05). Factor the lookup into a small helper `get_project_or_404(session, project_id)` so Plan 09's PATCH/DELETE reuse it. English comments, type hints (D-24).
  </action>
  <verify>
    <automated>uv run pytest backend/tests/test_projects_api.py -x</automated>
    <fails_when>non-zero exit, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests -x -q` exits 0
    - `grep -c "get_project_or_404" backend/src/yolo_trainer_api/routers/projects.py` returns at least 2 (definition + use)
    - `grep -c "Project not found." backend/src/yolo_trainer_api/routers/projects.py` returns at least 1
  </acceptance_criteria>
  <done>A single project can be fetched by id with a clear 404 for missing ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project shell (sidebar with back link + Overview) and Overview page, reachable from the card grid</name>
  <files>frontend/src/api/projects.ts, frontend/src/app/routes.tsx, frontend/src/features/projects/ProjectCard.tsx, frontend/src/features/project/ProjectLayout.tsx, frontend/src/features/project/ProjectOverviewPage.tsx, frontend/src/features/project/ProjectOverviewPage.test.tsx, frontend/src/i18n/locales/en/project.json, frontend/src/i18n/locales/ru/project.json</files>
  <read_first>
    - frontend/src/api/projects.ts, frontend/src/app/routes.tsx, frontend/src/features/projects/ProjectCard.tsx (current state)
    - frontend/src/test/render.tsx (Plan 03)
    - .planning/phases/01-runnable-skeleton-projects/01-CONTEXT.md D-06, D-11 and the "Specific Ideas" inside-project mock
  </read_first>
  <behavior>
    - rendering route /projects/7 with fetch returning project 7 shows its name, the "detect" badge, the description, a created date formatted by Intl for the active language, and the empty-state hint
    - project with description null shows the translated no-description hint
    - the sidebar shows the back link (to /projects) and exactly one section entry, Overview, marked active; there is no Settings/Images/Classes/Training/Models entry
    - in the grid, clicking a project card navigates to /projects/{id}
  </behavior>
  <action>
- `api/projects.ts`: add `projectKeys.detail = (id: number) => ['projects', id] as const` and `useProject(id: number | null)` (disabled when `id` is null) using `apiRequest<Project>('/projects/' + id)`.
- `routes.tsx`: nested route `/projects/:projectId` -> `ProjectLayout` with index route -> `ProjectOverviewPage`.
- `ProjectLayout.tsx`: parse `projectId` with `Number.parseInt` and treat non-positive/NaN as invalid (render the not-found state from Task 3 without fetching); two-column layout inside the existing AppShell main area — left column: `NavLink` "← Projects" (to `/projects`), then section `NavLink` "Overview" (active on the index route); right column: `<Outlet context={{ project }} />`. Only built sections are listed (D-11: no links to unbuilt sections). Leave the section list as an array of `{ key, to, labelKey }` so later plans append entries.
- `ProjectOverviewPage.tsx`: title with name + task-type `Badge`, description or `project:overview.noDescription`, created date via `new Intl.DateTimeFormat(i18n.language, { dateStyle: 'medium' })`, and a Mantine empty-state block with `project:overview.emptyHint` (text only, no links to unbuilt sections).
- `ProjectCard.tsx`: make the whole card a router `Link` to `/projects/${project.id}` (Mantine `Card component={Link}`), keeping name/badge/relative time.
- New namespace `project` (`en` + `ru`, identical keys): `nav.back`, `nav.overview`, `overview.noDescription`, `overview.createdAt`, `overview.emptyHint`, `notFound.title`, `notFound.body`, `notFound.back`.
No hardcoded strings (D-01); dark theme only (D-04).
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- --run</automated>
    <fails_when>non-zero exit, or ProjectOverviewPage.test.tsx reports fewer than 4 passed tests</fails_when>
    <human-check>End-of-phase batch (Plan 10): open a project from the grid, check the sidebar and Overview in both languages, reload the page on /projects/1 in the compose stack (nginx SPA fallback).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- --run` exits 0 with `ProjectOverviewPage.test.tsx` contributing at least 4 passed tests
    - `grep -c "useProject" frontend/src/api/projects.ts` returns at least 1 and `grep -c "ProjectLayout" frontend/src/app/routes.tsx` returns at least 1
    - `grep -Eci "settings|images|classes|training|models" frontend/src/features/project/ProjectLayout.tsx` returns 0 (no links to unbuilt sections in this plan)
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>A user can open any project from the grid into its sidebar shell and read its Overview.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Not-found states for missing/deleted projects and unknown routes</name>
  <files>frontend/src/features/project/ProjectNotFound.tsx, frontend/src/features/NotFoundPage.tsx, frontend/src/features/NotFound.test.tsx, frontend/src/app/routes.tsx, frontend/src/features/project/ProjectLayout.tsx</files>
  <read_first>
    - frontend/src/features/project/ProjectLayout.tsx and frontend/src/app/routes.tsx (Task 2)
    - frontend/src/api/client.ts (ApiError.status)
    - frontend/src/i18n/locales/en/project.json (notFound.* keys from Task 2)
  </read_first>
  <behavior>
    - /projects/999 with fetch returning 404 {"detail": "Project not found."} shows the translated not-found title/body and a link to /projects (no crash, no infinite spinner)
    - /projects/abc renders the same not-found state without calling fetch
    - /does-not-exist renders the generic NotFoundPage with a link to /projects
    - a non-404 API failure (500) on the project route shows the translated error title plus the API detail, not the not-found state
  </behavior>
  <action>
`ProjectNotFound`: translated title/body plus a button-link back to `/projects`. `ProjectLayout`: when `useProject` fails with `ApiError` status 404, or the id is invalid, render `ProjectNotFound`; other errors render the common error title plus `error.message` (D-05). `NotFoundPage`: generic translated page (reuse `project:notFound.*` or add `common`-free keys to the `project` namespace to avoid touching Plan 07's `common` files) with a link to `/projects`; register it as the `*` route in `routes.tsx`. Tests first in `NotFound.test.tsx`.
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- --run</automated>
    <fails_when>non-zero exit, or NotFound.test.tsx reports fewer than 4 passed tests</fails_when>
    <automated>npm --prefix frontend run build</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm --prefix frontend run test -- --run` exits 0 with `NotFound.test.tsx` contributing at least 4 passed tests
    - `grep -cE "path(=|: )[\"']\*[\"']" frontend/src/app/routes.tsx` returns at least 1 (catch-all route exists)
    - `npm --prefix frontend run build` exits 0
  </acceptance_criteria>
  <done>Stale, invalid or unknown links always land on a clear, translated not-found screen with a way back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path parameter -> API | `projectId` comes from the address bar (user- or link-controlled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | GET /api/projects/{project_id} | low | mitigate | FastAPI `int` path conversion (422 on non-integers); ORM `session.get` by primary key only |
| T-08-02 | Information Disclosure | 404 handling | low | accept | Single-operator app without auth; 404 reveals only non-existence of an id |
| T-08-03 | Tampering (XSS) | ProjectOverviewPage description rendering | medium | mitigate | Description rendered as React text (no HTML/markdown rendering); CSP from Plan 01 |
</threat_model>

<verification>
- `uv run pytest backend/tests -x` green
- `npm --prefix frontend run test -- --run` green; `npm --prefix frontend run build` green
</verification>

<success_criteria>
PROJ-02 "open": a user opens any project from the grid into the D-11 sidebar shell and sees its Overview; missing or unknown targets show a clear not-found state.
</success_criteria>

## Artifacts this phase produces (Plan 08)

- HTTP `GET /api/projects/{project_id}`; helper `get_project_or_404(session, project_id)`.
- Frontend: `projectKeys.detail(id)`, `useProject(id)`, routes `/projects/:projectId` (index Overview) and `*`, components `ProjectLayout` (section list array `{ key, to, labelKey }`), `ProjectOverviewPage`, `ProjectNotFound`, `NotFoundPage`; i18n namespace `project` (`nav.back`, `nav.overview`, `overview.noDescription`, `overview.createdAt`, `overview.emptyHint`, `notFound.title`, `notFound.body`, `notFound.back`).
- Tests: backend detail/404/422 cases; `ProjectOverviewPage.test.tsx`, `NotFound.test.tsx`.

## Edge coverage (spec-less probe fallback, resolved here)

- PROJ-02 / concurrency (read side): explicit — a project deleted elsewhere yields 404 and the translated not-found state (tested with a stubbed 404).

<output>
Create `.planning/phases/01-runnable-skeleton-projects/01-08-SUMMARY.md` when done.
</output>
